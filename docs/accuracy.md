# Accuracy

Every number Notchmeter shows is either a figure the vendor returned or an estimate built from a rule written down on this page. For each estimate this page states which files are read, which token buckets, which per-model prices and multipliers are applied, how duplicate lines are recognised, and exactly where the result is known to diverge from what the vendor bills. A golden-transcript test suite in the repository pins every rule, so a change in the numbers surfaces as a failing test rather than a surprise. Notchmeter does not show a number it cannot show its work for.

The Cost card carries every tool whose spend can be honestly derived, side by side; *Which tools report spend* below is the per-tool table, and the sections after it give each tool's rules in full. The Claude Code rules are those of Claude Code's own cost figure wherever Anthropic documents one. Primary sources, all read on 2026-09-01:

- [A] [Track cost and usage](https://code.claude.com/docs/en/agent-sdk/cost-tracking), Claude Agent SDK documentation.
- [B] [Manage costs effectively](https://code.claude.com/docs/en/costs), Claude Code documentation.
- [C] [Pricing](https://platform.claude.com/docs/en/about-claude/pricing), Claude Platform documentation.

The code is `Sources/Notchmeter/CostEngine.swift` (which tool is scanned, and the summary they add up to), `Sources/Notchmeter/ProviderCost.swift` (one tool's ranges, series, shares and source), `Sources/Notchmeter/CostCard.swift` (which tools the card carries, what each is worth in the range and the unit on show, and the donut's arcs), `Sources/Notchmeter/ClaudeCostScanner.swift` (Claude Code: reading, dedupe, digests, ranges, the daily history), `Sources/Notchmeter/ModelPricing.swift` (Anthropic prices, multipliers, overrides), `Sources/Notchmeter/CodexCostScanner.swift` and `Sources/Notchmeter/OpenAIPricing.swift` (Codex), `Sources/Notchmeter/CursorProvider.swift` (Cursor's export), and `Sources/Notchmeter/OpenCodeStore.swift`, `Sources/Notchmeter/OpenCodePricing.swift`, `Sources/Notchmeter/OpenCodeCostScanner.swift` and `Sources/Notchmeter/OpenCodeProvider.swift` (OpenCode, and the Go meter). The tests are `Tests/NotchmeterTests/CostGoldenTests.swift`, `Tests/NotchmeterTests/CostBreakdownTests.swift`, `Tests/NotchmeterTests/CodexCostTests.swift`, `Tests/NotchmeterTests/CostEngineTests.swift`, `Tests/NotchmeterTests/CostCardTests.swift` and the `CostScanning` and `CursorRound*` suites in `Tests/NotchmeterTests/ProviderParsingTests.swift` and `Tests/NotchmeterTests/CursorParsingTests.swift`.

## Which tools report spend

The Cost card shows one row per tool whose spend can be derived from something that tool publishes: the tool, the source the figure came from, that tool's share of the range and its total. Each row carries its own ranges (today, yesterday, week, month, 30 and 90 days), its own daily series, its own per-model shares, its own freshness stamp and its own error. The figure in the middle of the donut is the rows added up, and the donut around it is one arc per row, in that tool's own colour and sized by that row's share of the range; with one tool reporting it is the single ring the card has always drawn. The rows and the arcs follow the assistant order set in Settings, the same order the panel's cards and the readouts beside the notch use. The three units apply across every tool at once: Cost is dollars, Tokens is tokens, and $/MTok is each tool's own dollars over its own tokens — never the range's tokens apportioned by cost, which would print the same blended rate on every row. Each tool's own card repeats its spend as a line of its own ("$118.31 today · $6,600 over 30 days · local transcripts"), from the same figures.

A tool whose spend cannot be derived from a real source has **no row at all**, and so no arc either. Not $0 — a zero says "this cost you nothing", which is a claim, and it would be a false one. Nothing on this card is ever inferred from a subscription price, a seat count or a request count. A tool that can report spend but spent nothing in the range on show is in the legend at its own $0 and out of the donut, because there is no arc to draw.

*In the Cost card* (Settings, beside *Cost card shows*) ticks which of the reporting tools the card carries; all of them are ticked to begin with, and a tool that cannot report spend is never offered. Leaving one out takes it out of the donut, the legend and the total together, so the card always describes one set of assistants; the tool's own card still shows its spend line. *Show total spend* hides the card and every spend line with it.

**The detail block is the leading tool's, not the total.** The figure in the ring, the donut and the legend are the whole card: every carried assistant added up. The lines under the legend — the hour and its burn multiple, the tokens and their cache-read share, the cache tiers, the folders, the Claude weekly window, the session block, "since first use", and the line saying what kind of number these are — describe the **first assistant in the card's order**, whichever one the user dragged to the top, and each names it ("Cursor last hour $2.10 · 1.4x its 30-day average"). A line the leader's source cannot answer is simply absent: Cursor's export is day resolution so a Cursor lead has no burn line, it carries no folder so it has no "Top:" line, and the 1-hour against 5-minute cache split, the 5-hour session block, "since first use" and the tokens-per-percent metering appear only when Claude Code itself leads, because they are distinctions only its transcripts record. Two lines stay totals and say so in code: the headline in the ring, and the month against a monthly budget, which is set against every carried assistant at once and has no per-tool share to print. Pinned by `CostCardTests`.

**A carried tool that reports nothing says why.** A tool that is visible in the panel, ticked under *In the Cost card* and able to report spend, but that produced no figure, gets one muted line under the legend naming the reason the app actually knows: *Also read Cursor's usage events* switched off, the tool's own read error where the read failed or went stale, "no sessions on this Mac yet" where the tool is set up but has written nothing local, and otherwise "no spend read yet". Silence there read as "this cost nothing", which is a claim; the line is the difference between no spend and no reading. Nothing is guessed — there is no fifth reason. Pinned by `CostCardTests`.

| Tool | Source | What it prices | What it does not | On the Cost card |
|---|---|---|---|---|
| **Claude Code** | `localTranscripts` — this Mac's transcripts, priced here at Anthropic's published rates | every request a transcript records: five token buckets, per model, per project, ×1.1 US residency, web search | contracted rates, cloud regional premiums, usage-credit rates, other Macs, claude.ai chat | dollars, an hour and a burn multiple |
| **Codex** | `localSessions` — this Mac's session rollouts, priced here at OpenAI's published rates | every turn a rollout records: fresh input, cached input, whole output, per model, per folder | what a ChatGPT plan already includes, a turn naming no model, a model with no published rate | dollars, an hour and a burn multiple |
| **Cursor** | `billingExport` — Cursor's own usage-events export (on by default, can be switched off) | nothing: every dollar is the one **Cursor itself** put on that event | anything Cursor left unpriced, and anything older than the 30 days the export returns | dollars, day resolution, no hour |
| **GitHub Copilot** | `vendorCredits` — GitHub's own running count of the seat's AI credits, a cent a credit at GitHub's published rate, on seats GitHub meters in credits | nothing: the count is GitHub's and the rate is GitHub's | a seat GitHub does not meter in credits (a legacy request quota); the credits spent before Notchmeter first read the month; anything older than the daily history | dollars, day resolution, no hour, no model |
| **Gemini CLI** | none | nothing | — | **no cost** |
| **OpenCode** | `localMessages` — the turns OpenCode's own database records on this Mac | every assistant turn: an OpenCode Go turn at the Go page's own rates; any other turn at the cost OpenCode itself recorded for it; a turn it recorded at zero on an Anthropic or OpenAI login at that vendor's list rate | a turn recorded at zero by any other provider (a local model, say), which is named unpriced; turns on another Mac | dollars, an hour and a burn multiple |
| **Antigravity** | none | nothing | — | **no cost** |
| **Kimi Code** | none | nothing | — | **no cost** |

**Why GitHub Copilot shows AI credits, and only those.** Since 2026-06-01 GitHub bills Copilot by usage in AI credits — *"1 AI credit = $0.01 USD"* (docs.github.com, *Billing for individuals*, read 2026-09-20), with a monthly allowance per plan that resets on the first of the month and does not roll over — and the endpoint the editor plugin reads now carries the month's running count beside each quota snapshot (`quota_snapshots.*.credits_used`). That count, times a cent, is the one Copilot figure that is money, and it goes on the Cost card as a row with the source *AI credits* (`CostSource.vendorCredits`). What GitHub does not publish is *when* each credit was spent: the endpoint answers one month-to-date total, no per-day and no per-model detail. So the day series is what this Mac observed — every poll's rise over the last is laid on the day it was seen, the first read of a month sets the baseline rather than charging the whole count to one day, and a count that falls is the month's reset (`CopilotProvider.recordCredits`, pinned by `CopilotParsingTests`). Credits spent while the app was not running land on the day it next looks, and credits spent before it first looked are never charged to any day; the line under the legend says *no AI credits used since Notchmeter began counting* until the first rise. The row is tagged an estimate for that reason, though nothing in it is priced here. A seat whose snapshots carry no `credits_used` — a legacy premium-request quota — produces no row, and the line says *GitHub reports no AI credits for this seat*; the request counts stay counts on the Copilot card, because multiplying them by an overage rate would invent a bill for requests inside the allowance. Organisation billing (`usageItems[].netAmount` from `/orgs/{org}/settings/billing/usage/summary`, opt-in, org admins only) is still shown as GitHub's own on the Copilot card and still stays off the Cost card, because it is the whole organisation's month rather than this seat's. The plan allowances (Pro 1,500, Pro+ 7,000, Max 20,000, Business 1,900 and Enterprise 3,900 credits a month) are not assumed: GitHub publishes no per-seat ceiling on any endpoint, so a bar appears only where a snapshot carries an `entitlement`.

**Why Gemini CLI, Antigravity and Kimi Code show no cost.** All three meter a request allowance, not money. Google's Code Assist endpoint returns per-model or per-group windows and the fraction of each that is used; it returns no price, and no token count that a published rate could be applied to. Gemini's API rates would not help either, because a Code Assist or Antigravity seat is not billed at API rates. Kimi's usage endpoint returns counts of an allowance (`limit`, `used`, `remaining`) and ratios of it, never a price, and Moonshot publishes no per-request rate for a Kimi Code membership. There is nothing to derive a dollar figure from, so none is shown.

**Why OpenCode's row mixes three authorities, and says so.** OpenCode records, for every assistant message, its model, its provider, its tokens and a `cost` OpenCode worked out itself from its model catalogue (models.dev). That recorded figure is OpenCode's own, the way a Claude Code line's `costUSD` is Claude Code's, and it wins where it is above zero. Two cases do not take it: an OpenCode Go turn is priced at the Go page's own rates, because those are the rates Go's dollar limits are counted in and the meter below has to agree with the card; and a turn OpenCode recorded at zero on an Anthropic or OpenAI login (a subscription login, whose turns OpenCode does not charge) is valued at that vendor's list rate, the API-equivalent value the Claude Code row shows for a subscription too. Anything else recorded at zero is named unpriced rather than counted as free, since a zero from a provider OpenCode does not price says nothing either way. The line under the legend names the three rules in their order: *OpenCode at the Go page's rates for Go turns, a subscription's zero at list rate, otherwise the cost it recorded for each turn*. The full rules are in *OpenCode* below.

**Independence.** Each tool's scanner reads its own source, and the scanners run concurrently (`CostEngine.scan`). A Cursor export that is stale, refused or never fetched cannot delay or empty the Claude Code scan; a Mac with no Codex sessions simply has no Codex row; a tool switched off under Assistants is never scanned at all. Each row's timestamp is when *that* source was read, which is not when the app last drew the card, and a row whose read failed keeps its last figures and says what went wrong underneath. Pinned by `CostEngineTests`.

The sections from *What is read* to *The cache tier shift* are Claude Code's rules. Codex, Cursor, Copilot, Gemini CLI and Antigravity, Kimi Code and OpenCode have their own sections further down.

## What is read

Claude Code keeps one JSONL transcript per session under `projects/` in its config directory. Notchmeter looks in `$CLAUDE_CONFIG_DIR`, `~/.config/claude` and `~/.claude`, and reads every `*.jsonl` under each `projects/` it finds, including the `subagents/*.jsonl` files written beside a session. Subagent requests are real API requests and are priced like any other; the `isSidechain` flag is not consulted. Two more kinds of root are read the same way: Claude Desktop's Cowork sessions in `~/Library/Application Support/Claude/local-agent-mode-sessions` (the same line format, in per-session folders; their project is reported as "Cowork", and the same folder puts Cowork's tasks on the Sessions card, [Claude Cowork's tasks](#claude-coworks-tasks) below), and any folder the user adds under Settings › Assistants › *Also read transcripts from* (a `projects` folder, or a flat folder of session folders, such as another Mac's logs synced in). The same roots feed the activity check that sets the polling cadence.

Only files whose modification time falls inside the 30-day window are opened. Within a file, a line is an entry when it contains `"usage":{` and parses with a `timestamp`, a `message.usage.input_tokens` and a `message.usage.output_tokens`; everything else (user turns, tool results, summaries, progress lines) is skipped. Each entry also keeps the line's `cwd` reduced to its last path component (else the project folder's name decoded from Claude Code's `-Users-me-Developer-notchmeter` encoding, whose last segment is the folder name; a folder with a hyphen in its name comes out as its last piece, which the line's `cwd` corrects), its `sessionId`, `usage.server_tool_use.web_search_requests` and `usage.speed`. A `cwd` inside a git worktree is attributed to the repository the worktree was cut from rather than to the worktree's own folder (since 0.6.0): the worktree is recognised by its `.git` file's `gitdir: <repo>/.git/worktrees/<name>` pointer, read with FileManager rather than by forking git, or by the `<repo>/.claude/worktrees/<name>` path shape once the worktree has been removed and the transcript has outlived it; Codex rollouts and every vendor's hook payload go through the same rule. Days the durable history recorded before 0.6.0 keep the names they were written under.

Per-file results are cached in `~/Library/Caches/Notchmeter/claude-usage-cache-v5.json`, keyed by path, size, modification time and the pricing fingerprint (the snapshot date plus any override); the version suffix is bumped whenever a parsing rule changes so entries parsed under an older rule are never reused (v4 folded worktrees onto their repositories; v5, 2026-09-24, groups a response's lines by message id alone). Each cached file carries a **digest**: its priced cost, five token counts, per-model cost and per-project cost in quarter-hour buckets. A scan folds every unchanged file from its digest and reads entries only from files touched inside the current 5-hour block, which is what the last-hour and block figures need at minute precision, so entries are kept beside the digest only while the file is that recent and are dropped, from the cache file and from memory, once it ages out (a month of transcripts held at entry level was most of the app's resident size, and none of it was read); everything longer (today, yesterday, month, 30 and 90 days) is built from the digests, and quarter hours keep every local day boundary exact because every time zone offset is a multiple of fifteen minutes. Deduplication (below) happens within a file before its digest is built; a streamed response never spans two files. The cache is written after the first full parse and then at most once every ten minutes, because rewriting a cache of this size on every scan while a session is appending to its transcript was the app's largest CPU cost (README, "Energy"); files parsed since the last write are parsed again on the next launch, which costs a few milliseconds. The cache holds timestamps, model ids, token counts, message ids, session ids, project folder names and the `inference_geo` and `speed` values, never any prompt or response text.

### The daily history

Claude Code deletes transcripts after its `cleanupPeriodDays`, and Notchmeter reads 30 days, so without more the 30-day figure would shrink as files go and nothing older than 30 days would ever be known. After each scan the app appends one JSON line per changed day to `~/Library/Application Support/Notchmeter/daily-history-v1.jsonl` (day, tool, cost, the five token counts, per-model and per-project cost; never a path or a token). It lives beside the drain log rather than in `~/Library/Caches`, where builds before 2026-09-19 kept it: Caches is purgeable by the OS and excluded from Time Machine, and this file is the only record of any day older than the transcripts. A file left there by an older build is merged into the new one by the first process that reads or writes the history after the install, whether that is the app at its next launch or the `notchmeter` command, the status line or the MCP server running before it; until 0.5.0 only the app moved it, and a scan from one of the others in between created the new file from its own few days, which then hid the months in Caches from every reader and let a partly cleaned day be written at the transcripts' lower figure. On read the newest line per day wins, and a day is taken from the history when the history's total is larger than what the transcripts now price to, because a deleted transcript can only lower the live figure, never the real one. The 90-day range, "Since <first day>" and the 90-day daily series come from this file merged with the live 30 days. The file is compacted to one line per day once it passes a few thousand lines.

## The token buckets

Each entry contributes five counts, taken from these fields of `message.usage`:

| Bucket | Field | Price |
|---|---|---|
| Input | `input_tokens` | base input rate |
| Output | `output_tokens` | output rate |
| 5-minute cache write | `cache_creation.ephemeral_5m_input_tokens`, else the flat `cache_creation_input_tokens` | 1.25 × base input |
| 1-hour cache write | `cache_creation.ephemeral_1h_input_tokens` | 2 × base input |
| Cache read | `cache_read_input_tokens` | 0.1 × base input (0.025 × on Fable 5.1 and Mythos 5.1) |

The cache multipliers are Anthropic's, from the prompt-caching table in [C]: a 5-minute cache write is *"1.25x base input price"*, a 1-hour cache write *"2x base input price"*, a cache read *"0.1x base input price (0.025x on Claude Fable 5.1 and Claude Mythos 5.1)"*, and *"These multipliers stack with other pricing modifiers, including the Batch API discount and data residency."* The two tiers matter on a subscription because, per [A], *"On a Claude subscription within your plan's included usage, you get the 1-hour TTL on your own turns, and on some of the helper requests Claude Code makes beside them, without setting this variable, and Claude Code drops those turns to the 5-minute TTL once you're drawing on usage credits."* When a transcript has only the flat `cache_creation_input_tokens` (older Claude Code versions), it is priced as a 5-minute write.

Thinking tokens need no bucket of their own: `output_tokens` already includes them and, per [B], *"Thinking tokens are billed as output tokens"*.

## Per-model rates

Dollars per million tokens, as coded in `ModelPricing.swift` from the table in [C] on 2026-09-01. Cache columns are derived from the base input rate with the multipliers above.

Two things the table deliberately has no column for, checked on 2026-09-20. The code-execution container fee (billed per container-hour on the API's code-execution tool) was checked against Claude Code's transcripts and is immaterial for Claude Code: its Bash runs locally, no transcript line carries a container id or a container-hours field, and nothing in the usage object prices one, so there is no fee to add and none is added. And Claude 4.6 and later bill the full 1M-token context window at the standard per-token rates: there is no long-context tier for Claude, so `ModelPricing` has no token threshold and a 900K-token request is priced the same per token as a 9K one; the `exceeds_200k_tokens` flag in the status line is decoded by nothing because it changes no rate. (The one long-context tier in the app is OpenAI's, for Codex, below.)

| Model id prefix | Input | Output | 5m write | 1h write | Cache read |
|---|---|---|---|---|---|
| `claude-fable-5-1`, `claude-mythos-5-1` | 10 | 50 | 12.50 | 20 | 0.25 |
| `claude-fable-5`, `claude-mythos-5` | 10 | 50 | 12.50 | 20 | 1.00 |
| `claude-opus-5`, `claude-opus-4-8`, `-4-7`, `-4-6`, `-4-5` | 5 | 25 | 6.25 | 10 | 0.50 |
| `claude-opus-4-1`, `claude-opus-4` | 15 | 75 | 18.75 | 30 | 1.50 |
| `claude-sonnet-5` | 2 | 10 | 2.50 | 4 | 0.20 |
| `claude-sonnet-4-6`, `-4-5`, `-4`, `claude-3-7-sonnet`, `claude-3-5-sonnet` | 3 | 15 | 3.75 | 6 | 0.30 |
| `claude-haiku-4-5` | 1 | 5 | 1.25 | 2 | 0.10 |
| `claude-3-5-haiku` | 0.80 | 4 | 1.00 | 1.60 | 0.08 |
| `claude-3-haiku` (no longer on the price page; kept for old transcripts) | 0.25 | 1.25 | 0.3125 | 0.50 | 0.025 |

Sonnet 5 is priced at $2/$10 because, per [C], *"The $2/$10 per million input/output token pricing for Claude Sonnet 5, announced at launch as introductory pricing through August 31, 2026, is now the standard price."*

**Fast mode.** A line whose `usage.speed` is `"fast"` on Opus 5 or Opus 4.8 is priced at $10/$50 per million, twice the standard rate, with the cache multipliers applied to the doubled input rate; every other model ignores the marker. The field is present on current transcripts (`"speed":"standard"` on the lines on this Mac).

**Overrides.** Two tables replace the built-in one, by normalised model prefix, longest prefix first: Claude Code's own `modelPricing` in `~/.claude/settings.json` (or `$CLAUDE_CONFIG_DIR/settings.json`), so the estimate matches the figure Claude Code shows when a contracted table is in effect, and then Notchmeter's own `~/Library/Application Support/Notchmeter/pricing-overrides.json`, which wins on a clash. Both take `{"<model id>": {"input": 5, "output": 25, "cacheRead": 0.5, "cacheWrite": 6.25, "cacheWrite1h": 10}}` in dollars per million (snake_case and `_tokens` spellings are accepted; a missing cache rate derives from the input rate with the standard multipliers). An override changes the pricing fingerprint, so every cached digest is re-priced on the next scan. Pinned by `CostBreakdownTests`.

**Snapshot and freshness.** `Sources/Notchmeter/Resources/pricing-snapshot.json` is the same table as a document, dated; a unit test checks it against `ModelPricing.table` and `ModelPricing.snapshotDate`, and `.github/workflows/pricing.yml` fetches the pricing page weekly and diffs the per-model rows against the snapshot, so a price change fails a scheduled job rather than going unnoticed. The honest caveat: that job needs network access in CI and a page whose layout it can parse; when the page changes shape the job fails on a parse error, which is still a signal.

A model id is normalised before lookup: lower-cased, a Bedrock or Vertex prefix (`anthropic.`, `us.`, `eu.`) and a `@date` suffix are stripped, and the longest matching prefix wins, so `claude-opus-4-1-20250805` never matches `claude-opus-4`. An id that matches no prefix but names a known family (`fable`, `mythos`, `opus`, `sonnet`, `haiku`) is priced at that family's newest rate. That fallback is a guess made so a freshly released model prices roughly right until the table is updated; it is listed here so it is not mistaken for a fact.

## One entry per response

Claude Code writes a streamed response to the transcript as several lines, one per content block, each carrying the same `message.id`, the same `requestId` and the same `usage`. [A]: *"When Claude uses multiple tools in one turn, all messages in that turn share the same ID, so deduplicate by ID to avoid double-counting."*

Rule: lines are grouped by `message.id`; a line without one is kept on its own. Within a group, the line whose `output_tokens` is largest is kept and the rest are dropped. Input and cache counts are identical across a group, so nothing else changes. The next section is why the largest, not the first.

Until 2026-09-24 the key was `message.id` + `requestId`, the pair ccusage groups by, and a line missing either was kept on its own. Claude Desktop's Cowork sessions broke that rule without anyone noticing: their transcripts spell the request id `request_id` on some lines and leave it off others, so each such line stood alone and a streamed Cowork response was priced once per content line, its input and cache tokens two or three times over. Counted on this machine on 2026-09-24 (counts only, no text read): across 85 Cowork transcripts, 8,316 usage lines, of which 4,376 carried `requestId`, 2,494 `request_id` and 1,446 neither, every one with a `message.id`; across 5,954 Claude Code transcripts, 150,619 usage lines, every non-synthetic one carrying `requestId`. In neither set was a `message.id` ever seen with two request ids, and in none of the 2,102 Cowork and 51,341 Claude Code multi-line groups did input or cache counts differ, so the request id never told apart two lines the id had joined, and [A]'s rule, deduplicate by id, is the one applied. Claude Code's own figures are unchanged by the move; Cowork's fall to once per response. Pinned by `CostGoldenTests` (`coworkLinesCollapseWhateverTheRequestIdIsCalled`, `theDedupeKeyIsTheMessageIdWhateverTheRequestIdIsCalled`).

## The output_tokens placeholder

[A] discloses that a line's output count is not what it looks like: *"Claude Code builds each assistant message from the usage the API reported when the response began, so the message's `output_tokens` is only the count the API had reported at `message_start`, before the response was generated. One API response can produce several assistant messages, and every one of them carries that same placeholder."* And: *"The API reports the real output count at the end of the response, and Claude Code adds it to the result message."* The result message is an SDK stream object, not a transcript line, so a transcript scanner cannot read it. What it can read is the last line Claude Code writes for a response after the stream has ended, which carries the same real count.

Measured on this machine on 2026-09-01, across the 3,501 transcripts touched in the previous 30 days: 31,115 response groups, 22,186 of them spanning several lines. In every one of those 22,186 the final line had the largest `output_tokens` and the earlier lines the placeholder, and in none did input or cache counts differ across the group. Keeping the first line of a group, which is what ccusage-derived tools do, would have counted 17,975,458 output tokens; keeping the last counts 24,339,282, 35% more.

What remains undercounted: a response that never finished (Claude Code interrupted, a crash, a network drop) has only placeholder lines, so its output tokens are counted at the `message_start` value, typically under ten, while its input and cache tokens are right. Such groups cannot be told apart from genuinely short replies, so no correction is attempted; on the machine above 4,969 groups had a best output count of ten or fewer, and some unknown share of those are cut-off responses.

## The residency multiplier

Anthropic bills US-only inference at a premium, and Claude Code models it. [C]: *"For Claude 4.6 and later models, specifying US-only inference through the `inference_geo` parameter incurs a 1.1x multiplier on all token pricing categories, including input tokens, output tokens, cache writes, and cache reads. Global routing (the default) uses standard pricing."* [A]: *"One billing rule the SDK does model is data residency pricing. When a response's `usage` reports `inference_geo: "us"`, the SDK multiplies the list price of that response's tokens by 1.1. Per-request fees such as web search aren't multiplied."* [B]: *"For a response from the Claude API billed at the 1.1× data residency rate, Claude Code multiplies the list price of that response's tokens by 1.1 in the session cost figure. … Before v2.1.239, Claude Code didn't apply the 1.1× to those responses, so the session cost figure was lower than the bill."*

Rule: when `message.usage.inference_geo` is exactly `"us"`, the line's five buckets are priced at list and the sum is multiplied by 1.1. Any other value (`"global"`, `"not_available"`, a missing field, or anything unrecognised) is priced at list. On a Claude subscription the field is written as `"not_available"`. Per [C], *"Earlier models do not support the `inference_geo` parameter and always use standard pricing"*, so a line for a model older than 4.6 never carries `"us"` and is never multiplied.

## Web search

Web search is a per-request fee, not a token rate. [C]: *"Web search is available on the Claude API for $10 per 1,000 searches, plus standard token costs for search-generated content."* Rule: `usage.server_tool_use.web_search_requests` × $0.01 is added to the line's token cost, after the residency multiplier, which per [A] never applies to per-request fees. Web fetches carry no fee. Pinned by `CostBreakdownTests.webSearchesArePricedPerRequestAndNeverMultiplied`.

## Lines with their own costUSD

A line may carry a `costUSD` (at the top level or inside `usage`); older Claude Code versions wrote it and the SDK still computes it. When present it is taken as the line's cost and no pricing is done, so the residency multiplier is never applied twice. Note [A]'s warning about the field: *"The `total_cost_usd` and `costUSD` fields are client-side estimates, not authoritative billing data. The SDK computes them locally from a price table bundled at build time, unless a `modelPricing` table is in effect."* Current Claude Code writes no `costUSD` into transcripts (none of roughly 140,000 usage lines on this machine had one), so in practice every line is priced here.

## Unknown and synthetic models

A line whose model is `<synthetic>` is a message Claude Code fabricated locally (an interrupted-turn notice, for instance); it contributes $0 and is not reported. A line whose model matches neither a prefix nor a family also contributes $0, and the model id is listed on the Cost card as "Unpriced: …" and in `--probe` output as `unpriced=[…]`, so a silent zero never hides a new model.

## The combined "All models" window

A plan that splits its allowance by model — Cursor Enterprise's "Cursor models" and "Other models", Anthropic's per-model weekly caps — can also show one derived window called **All models** (`CombinedWindow.swift`). It is the only window in the app that no vendor published, so it is tagged *inferred* on the card and captioned "Combined from the windows below", and it obeys rules that stop it claiming precision the source does not have:

- Only windows that publish a used fraction are combined. A window with no limit contributes nothing; it is never read as zero.
- Fewer than two model-scoped windows report and there is no combined window at all, so nothing synthetic appears where there is nothing to combine.
- Where the vendor already publishes the total those windows are shares of, **that figure is adopted exactly and never recomputed**. Cursor's "Included usage" *is* the plan total; adding "Cursor models" and "Other models" back together would round differently from Cursor's own arithmetic and disagree with the dashboard. A window counts as the total only when it is not scoped to a model, covers the same period and reset as every model window it would cover, and does not read below any of them — one that reads lower is not their parent, whatever else it is.
- With no such total, the figure is the **highest** of the model windows, never their mean. An average of a maxed-out model and an untouched one would show headroom that does not exist.
- The reset is the **soonest** among the windows covered, so the countdown never runs past the first cap to bite.
- No money is added up. The combined window carries no dollar figure: a share of a total and a dollar amount are not the same arithmetic, and summing shares across models would double-count the total's own dollars.

On a seat Cursor meters nothing for (an Enterprise seat billed on-demand), Cursor still answers its two model meters, and they read 0 % all cycle however much the usage export shows the seat spending: on 2026-09-20 one read *"Cursor models 0 %"* against $8.76 of export that day, and "All models", combined from the two, read 0 % with them. **A 0 that money is flowing past is not a figure.** Once the *Today's spend* window exists — thirty days of export dollars with no headline figure — a model meter that still reads exactly 0 loses its fraction (`CursorProvider.withoutDeadSplits`) and its caption says why; with no figure it contributes nothing to "All models", by the first rule above. A meter reading anything above 0 is alive and is left as it is, so one that starts counting comes straight back. Rings chosen for the two meters in Settings are not rewritten: a choice that would draw only empty rings while *Today's spend* is on show yields to the data (`RingSelection`), which puts *Today's spend* on the outer ring, and the stored choice is honoured again the day a meter counts. Without a comparison window on show an explicit choice wins as it always has, empty ring or not.

## When a vendor publishes two figures for one window

Cursor's usage summary answers the same question twice for its plan and team-pooled windows: `totalPercentUsed`, and the `used`/`limit` pair the note under the bar is written from. On most plans they agree to the cent. On Enterprise they need not — one account read `totalPercentUsed` 55 while `used` and `limit` were both $20, the whole allowance gone, with the two model splits beside it reading 47 % and 100 %.

**The window is as spent as its furthest-along figure says** (`CursorProvider.share`, pinned by `CursorParsing`). The account's own billing export is what settles it: Cursor marks a usage event `On-Demand` only once the included allowance is spent, and that account had 47 On-Demand events worth $116 in the very billing cycle the 55 % was reported for, and $2,306 in the cycle before it. Included was gone. A bar at 55 % said half an allowance remained while every third-party request was already being charged for.

Neither field can be shown to be the wrong one from inside a single reading, so the choice rests on which way it is safer to be wrong, and the two failures are not symmetric. Under-reporting a spent window hides a meter that is actively charging money — the exact thing this app exists to catch. Over-reporting one warns early.

Two things about Cursor's Enterprise shape remain **unknown and are not guessed at**:

- What `totalPercentUsed` is a percentage *of*. It is the figure cursor.com's own dashboard shows, so a user reconciling against the dashboard will see the meter disagree with it; that is accepted here in exchange for not under-reporting a spent cap.
- What the model splits are percentages *of*. `autoPercentUsed` 47 and `apiPercentUsed` 100 sum to 147, so they are not two shares of one total, and "Included usage" is not their parent. The billing export agrees from the other side: on the day of that reading, requests to Cursor's own models were still billed as Included while a third-party model's request went On-Demand — separate meters, not slices. The caption now says exactly that much and no more — *"Metered apart from the included total"* — where it used to say *"Share of the plan's included usage"*, which answered this question with a guess that the arithmetic rules out. What each figure is a percentage of stays unestablished, and nothing infers it.

The dollars are printed under the bar exactly as the summary gave them, so where the two figures disagree the disagreement stays visible on the card instead of being folded away into one number.

## When a reset is not a fixed instant

A window's reset is treated as a period, not a timestamp. Claude's windows arrive carrying a moment that moves on every read: three windows of one reading were observed a millisecond apart, and one window's reset wandered inside a two-second band across twenty minutes while its figure never moved — the signature of a moment recomputed from a remaining duration rather than quoted from a calendar. Compared exactly, the same period reads as a new one on every read.

So every comparison of two resets goes through `ResetPeriod.same`, which draws the line at ten minutes — the tolerance `NotificationScheduler` already used for a Codex snapshot whose reset is measured from when it was written. No real reset is ever that close to the one before it, because the shortest window the app meters is five hours. Exact comparison had four consequences, all silent: the drain log wrote a row for every window on every read however still the figure was (a few KB a day became a few hundred), `RunOutInterval` discarded every consecutive pair of rows as spanning a reset so no run-out estimate could form, `CombinedWindow` could not recognise a tool-wide window as the parent of its own model windows, and a watched reset was dropped and re-watched on each read. Pinned by `DrainLogRules`.

## Dials and the hour clock

Two ways of drawing a window arrived on 2026-09-24 (Settings › Appearance › Theme; `UsageGauge.swift`), and neither shows a figure the bars do not. Both are pictures of fields the card already prints, and they take those fields' sources with them: a legend row beside a dial is the same row a meter stands in, tag and all, so a window marked *status line*, *snapshot* or *inferred* on the bars is marked the same way beside its ring.

- **The dial** (*Usage style › Gauges*) draws each window's used fraction as an arc and the pace tick at the share of its period gone, from the window's own reset and period, as the meter does. At most three rings, in the card's order; a window past the third, and any window with no figure, keeps its meter and its words, so nothing is drawn as an empty ring that could read as 0 %. The figure in the middle is the one the Simple row names (`SimpleFigure.window`: the window furthest behind pace, else the most used), and it is withheld while the screen is shared, as the row's figure is. A ring on track or behind takes the pace colour, as a meter's fill does. The Simple row's small dial (20 points) draws the same rings without the pace tick, which across a 2-point ring reads as a line struck through the dial rather than a mark on it (`UsageDial.drawsTicks`, 40 points and up; the pace is still in the row's own words and colour), and it is left out while every ring would stand at nothing used, since a bare track beside "0%" reads as a spinner (`UsageDial.rowRings`). The dial, the legend swatches and the clock are sized relative to the text styles beside them (`@ScaledMetric`), so they keep their proportion to the rows at whatever size the text is drawn; they are pictures of the same figures at every size (2026-09-24).
- **The hour clock** (*Draw hour limits on a clock*, off by default) is drawn beside the reset of a window whose period is shorter than a day — the five-hour session is the case it exists for — on the card, and on the Simple panel once a row is open onto its card: the closed row names no reset for a clock to stand beside, and a clock next to a bare "14%" read as a second picture of the usage. Its filled part is the time still to run, `(reset − now) ÷ period`, capped at the period: a reset further off than the period reads full. A reset already past draws no clock, as it draws no pace tick (`Pace.elapsedFraction`; 2026-09-24): it is a window that has started over without the app being told — a Codex snapshot written before its reset, a Claude reading cached across one — and its time left is unknown, not zero, so an empty face saying "no time left" would be a guess. It needs the vendor's reset instant and a period. With no reset it is drawn only where the reset line beside it already says *Not started* — nothing used and no reset (`ResetText.line`) — and then it is full, the whole period ahead; a window with usage and no reset gets no clock. A window of a day or more keeps its words, because a clock face says hours. The period is whatever the window carries: where it was inferred rather than published the row already says *inferred*, and the clock is exactly as inferred as the tick. Pinned by `HourClockTests` and `UsageDialTests`.

The theme itself (Black or Paper, the material, the accent) moves colours and never a number. Its one rule is contrast: every line of text on the open panel reads at 4.5:1 or better and every mark at 3:1 on the sheet, a card and the "needs you" wash, with a translucent material judged over a white window behind it, and under Increase Contrast the text on the selected pill at 7:1 on either face (the accent lifted on the black panel, darkened on Paper; 2026-09-24); colours move in lightness alone, by the least that reaches that, and the audit that holds every combination to it is `ThemeContrastTests`. The rules and the measured colours are in [features.md](features.md#themes).

## Subscription and API billing

Claude Code's own figure is an API-price estimate and Anthropic says so. [B]: *"The Session block in `/usage` shows API token usage and is intended for API users. Claude Max and Pro subscribers have usage included in their subscription, so the session cost figure isn't relevant for billing purposes."* And: *"Claude Code computes the dollar figure locally from token counts at list price, unless a `modelPricing` table is in effect."*

What the Cost card means on each kind of account:

- **Pro, Max, Team, Enterprise seat.** Nothing here is a bill. The figure is the API-equivalent value of the work your sessions did, useful for pace, for comparing days and for judging what a plan is worth. Usage credits drawn past the plan limit are billed by Anthropic at its own rates; the card does not know which lines were inside the allowance and which drew credits.
- **API key or Console.** The figure is an estimate at list price. [B]: *"By default, Claude Code computes every cost figure it shows developers at list price, so if your organization pays contracted rates, the figures in `/usage`, the status line, and OpenTelemetry don't match your bill."* Notchmeter reads Claude Code's `modelPricing` table and its own overrides file (above), so contracted rates are modelled exactly when one of those carries them and not otherwise. The authoritative number is the Usage page in the Claude Console.
- **Bedrock, Vertex, Foundry.** Priced at Anthropic list. Partner platforms have their own price lists; per [C], *"Regional and multi-region endpoints include a 10% premium over global endpoints"* on Bedrock and Google Cloud, which is not modelled.

## Known divergences

Things a bill can contain that this estimate does not, in the order they are likely to matter:

1. **Interrupted responses** are output-undercounted, as described above.
2. **Contracted rates** (unless Claude Code's `modelPricing` or an override supplies them), **regional cloud premiums** and **usage-credit rates** as above.
3. **Price changes.** The table is a snapshot of [C] on 2026-09-01. Prices move; the golden tests will not notice a price change, only a change in how the code applies them. The weekly pricing workflow is what notices.
4. **Other machines and claude.ai.** Only transcripts on this Mac, plus the folders you add and Cowork's, are read; claude.ai chat is never priced.
5. **Currency.** Every figure is computed in US dollars; *Show costs in* converts with a rate the user types and a symbol from the locale. The rate is never fetched, so a converted figure is only as current as the rate.
6. **Cursor's percentage fields.** The dashboard's cycle endpoint (`get-current-period-usage`, below) carries `totalPercentUsed`, `autoPercentUsed` and `apiPercentUsed` beside the cents. Cursor staff have said they *"reflect a different internal metric"* and are *"not calculated as a simple totalSpend / limit"* (forum thread 168210, 2026-08-13), and between 2026-08-11 and 2026-08-17 the three froze backend-side while the cents kept counting (*"The usage counter freeze is fixed. It was a backend issue with how totals were aggregated for display"*, 2026-08-17). The included ring is therefore drawn from `includedSpend / limit` and never from those fields; the summary's own `totalPercentUsed` is still read, and reconciled by the furthest-along rule above, because a frozen percentage can only under-report there and the cents beside it win.
7. **Copilot's day series.** The AI-credit count is month-to-date; the day each rise lands on is the day this Mac saw it (*Why GitHub Copilot shows AI credits* above).

Modelled since 2026-09-02, and so no longer divergences: the web-search fee and fast mode (above).

Token counts themselves are never estimated: every count comes from the API's own `usage` object as Claude Code recorded it, so tokenizer differences between models do not enter a cost or a count — each model's tokens are counted by its own tokenizer, and priced at its own rate. They do enter a comparison across models. Anthropic's pricing page says Claude 4.7 and later, and the Mythos/Fable line, tokenize with a vocabulary that yields approximately 30 % more tokens than Claude 4.6 and earlier for the same text, so a million tokens is not one unit across that line: the **$/MTok** mode adds a quiet caption naming the costliest newer-side model when the range holds models on both sides, and the switch-models advice adds the same clause when the two models it names straddle it. A name that cannot be placed — the vendor's bare "Opus" or "Sonnet" label with no version, or any non-Claude model — is not placed on either side, and nothing is said. Pinned by `TokenizerTests`, `CostCardTests` and `AdvisorTests`.

## Ranges, projects and the block

- **Today, Yesterday, 30 days, 90 days, Month** are calendar-day ranges in the local time zone, built from the digests and the daily history. Month is the calendar month to date.
- **Week** starts where the live Claude weekly window started (`seven_day.resets_at` minus seven days, from the last reading; the calendar week when there is none), built from the quarter-hour digests so the boundary is exact. "$1.58 per 1% of weekly" is the week's cost divided by the weekly window's used percentage from the same reading.
- **The block** is the current 5-hour window, aligned to `five_hour.resets_at` so the Cost card and the Session meter describe the same period, priced from entries; "tokens per minute" is the block's tokens over the minutes since its first entry.
- **By model** ranks the range's models by cost, the fifth and beyond folded into Other; **by project** does the same by folder name. The sparkline's tooltip names each day's top model.

## The last seven days

The Simple panel's Cost row carries today and the six days before it: a strip of seven bars beside the row's figure, the days in words when the pointer rests on the row, and a chart split by assistant when the row is opened (the Detailed Cost card draws the chart under *Show details*). It is the card's own data and adds nothing to it: each carried assistant's per-day series (`ProviderCost.daily`), the records the card's Today and Yesterday are built from, priced the same way and from the same sources the legend names (local transcripts at list rates, the billing export, the AI-credit count); no figure is estimated, fetched or carried over for it. The days are calendar days in the local time zone, matched by their date key as the Usage Dashboard matches them, so a day whose daylight saving begins at midnight still lines up with its record. Seven calendar days ending today are not the card's **Week**, which begins where Claude's weekly window began, so the two totals can differ, and each is labelled as what it is ("7 days $1,984" against the range bar's *Week*). The bars are in the card's unit: tokens under *Tokens*, dollars under *Cost*, and dollars under *$/MTok*, since a rate per million tokens does not add up across days and has no height to give one. A token figure on the week is the bare compact count ("7.4M", "820K"), the same letters in every language (`Money.compactCount`); the row's own figure beside it carries the unit in the reader's language, so the week's headline fits the one line the chart gives it in German and Russian as it does in English. A week in which no carried assistant spent anything draws no strip, rather than seven bars saying nothing. Pinned by `WeekSpendRules` (written 2026-09-24).

## Scrolling a ring onto another window

A scroll over a readout, or VoiceOver's adjustable action on it, changes which window the outer ring shows (`RingCycle`), which is the same choice the ring pickers under Settings › Assistants make. It only ever offers windows the card shows with a figure: a window with no figure would turn the ring into a dashed circle, and a hidden one would be revealed on the card by a gesture nobody meant as a setting. The label that names the new window for a moment carries the window's own source tag beside its figure, as the card does (`inferred` for a `localEstimate` window such as a budget, `status line`, `headers`, `snapshot`; nothing for the vendor's endpoint), so a figure this Mac worked out is never shown as the vendor's, and it carries no figure while the screen is shared. Pinned by `RingCycleRules` (written 2026-09-24).

## Burn rate

The Cost card's "Claude last hour $8.40 · 6x its 30-day average" line, which is the card's leading tool's own hour, and the Advice strip's "Claude Code burned $8.40 this hour — 6x its 30-day average." from three times up, are built from the same entries:

- **Last hour** is the priced sum of entries whose timestamp is within the past 60 minutes.
- **Typical hourly** is the mean priced cost of an active hour across the 30-day window: the window's total divided by its number of active hours, an active hour being any clock hour (UTC-aligned) with at least one entry. Hours you were not using Claude Code do not pull the average down. It was a median until 2026-09-02; agent work is bursty, most active hours cost cents and a few cost tens of dollars, so the median sat near zero and an ordinary hour read as "83x your usual". The mean is the figure the multiple is named after, and a burst is now measured against the whole month.
- **Burn multiple** is last hour ÷ typical hourly, shown with one decimal below ten ("2.3x") and none from ten up ("18x"). It is not shown until five active hours exist and the average is above zero, so a fresh install or an all-unpriced history never shows "∞x".

Each tool computes all three from its own entries and is named in its own line, so a hot hour says which tool was hot. Only a source whose entries carry a time of day can answer at all: Claude Code's transcripts and Codex's rollouts do, and Cursor's export is day-resolution, so Cursor reports no hour and never appears in a burn line. Where no single tool is over the line but every tool together is, one line is said for the total instead ("Every tool together burned $12.60 this hour — 3.4x your 30-day average"), and only when more than one tool is spending.

## Peak hours

Anthropic applies tighter session limits on weekdays between 05:00 and 11:00 Pacific. That window is reporting, not documentation: The Register described it on 2026-03-26 from Anthropic's announcement, and Anthropic's support article on usage limits does not publish the hours. So it is a preference (the hours and the switch under Settings › Advanced › Peak hours; which assistants they apply to under each assistant's options in Settings › Assistants; on for Claude only by default, editable, off for every other vendor because none has announced one), not a constant the meter vouches for. What it changes: inside the window the session projection assumes the peak rate observed in the drain log; the advice names the next off-peak start for a long job ("Off-peak in 1h 20m: start the long job then"); and the run-out interval below keeps peak and off-peak rates apart when it has enough of each. Pinned by `PeakHoursTests`.

## The run-out interval

The even-burn projection and the last-hour drain both answer with one time. The interval answers with two, from the drain log: every hour of the last seven days in which the window moved gives a rate (used fraction per hour, from the log's rows, reset boundaries excluded); the 20th and 80th percentiles of those rates give the latest and the earliest run-out for what is left. Which time is shown is one rule, `RunOutInterval.presentation`, for the card and the advice (and the notification, which carries the advice line) alike: a range ("Runs out 2:10 PM–4:40 PM" on the card, "between 2:10 PM and 4:40 PM" in the advice) when the edges are more than fifteen minutes apart, one time at their midpoint when they are not, and, when only the fast edge falls before the reset, the fast edge: the card says "Runs out from 2:10 PM, or lasts to the reset", and the advice names that same edge as a single time ("today at 2:10 PM, 2h 50m before reset") because its two strings have no third form, so the two agree on the time and only the card carries the hedge. Until 0.6.0 the card took the midpoint and the advice the earliest edge, so the panel named two times for one event. Notifications fire on the earliest edge, which is a threshold rather than a display. Fewer than four rates give no interval, a run-out past the reset gives none, and with peak hours on and at least four rates on each side the estimate uses only the rates that match the current side of the boundary. It is a description of your own recent hours, not a model of the vendor's metering. Pinned by `RunOutTests`.

## Session metering

Anthropic meters the session window in something other than tokens, and the ratio moves: the same work costs a different share of the window on different days. Notchmeter measures the ratio it can see: the current 5-hour block's tokens (from the transcripts, aligned to `five_hour.resets_at`) divided by the session window's used percentage from the same reading, "1.1M tokens per 1% of session", recorded once a day in the daily-history file (`sessionTokensPerPercent`) and compared with the median of the last 30 days' values (at least five are needed). When today's figure is half the median or less, that is, the session is metering at least twice as heavily as usual, the Cost card says so and the Advice strip carries "The session is metering about 2.1x heavier than your norm"; the *When the cache tier or the metering shifts* notification fires for it once a day. A ratio under 2 % of the window used, or from a block under 50K tokens, is not recorded, because the division is too noisy there. And a multiple of 6x or more is not called heavier metering at all: the block's tokens are this Mac's, while the window's used share is the whole account's, so past that point work on another Mac or on claude.ai explains the gap far better than a change in metering.

## The cache tier shift

Per [A], a subscription gets the 1-hour cache TTL inside the plan's included usage and drops to the 5-minute tier once it draws on usage credits, and the 5-minute tier re-caches the prompt more often, which costs more per turn. The transcripts show which tier each write used (`ephemeral_1h_input_tokens` against `ephemeral_5m_input_tokens`), so the share of today's cache writes on the 1-hour tier is compared with the same share over the last 30 days; when today is at least 25 points lower and today has at least 100,000 cache-write tokens to judge by, the Cost card notes the shift and the Advice strip says "Cache writes today are 12% 1-hour against a 30-day norm of 64%". It is an observation of the tier, not of the credits: the transcripts do not say why the tier changed.

## The prompt cache

The transcripts record that a cache write happened, never why, and a large write with no read is also what the first turn of every session and every `/clear` looks like, so no miss is ever inferred from them. The figures come from Claude Code itself: since 2.1.251 its [status line](https://code.claude.com/docs/en/statusline) carries a `prompt_cache` object per session with `requests`, `misses` (requests that re-processed content the cache already held: more than 5 % and at least 2,000 tokens of what could have been read from cache, with no compaction or tool-result clearing to explain it), `expected_rebuilds` (rebuilds after a compaction, not counted as misses), `hit_ratio`, `miss_recache_tokens` (the tokens the misses wrote back), and since 2.1.260 `last_miss_cause` (`tools_changed`, `system_prompt_changed`, `ttl_expired_5m`, `likely_server_side`, with the tools added and removed or the system prompt's change in characters) and `miss_causes`. Subagent requests are not counted by Claude Code. `Notchmeter --statusline` forwards the object whole ([docs/hooks.md](hooks.md#the-status-line)); the app keeps it on the session (`AgentSession.promptCache`, a `PromptCacheStats`) and prices the rewritten tokens once, at the session model's cache-write rate for the tier the cache is on (`cache_write_1h` when `ttl` is `1h`, else `cache_write_5m`, the same multipliers as above; nil for a model the table does not know, never a guess). Every count is Claude Code's running total for that session; the Cost card's caption ("Prompt cache: 4 misses today · 310K tokens (~$0.93) rewritten · cause: tools changed") and the report's `promptCache` object add up the Claude sessions heard from today, and the advice line adds up those heard from in the current 5-hour block, so a session that began before the span carries its earlier misses into it. The advice line fires at three misses in the block or 200,000 rewritten tokens (`Advisor.promptCacheMisses`, `Advisor.promptCacheRewrittenTokens`), names the cause of the newest miss as Claude Code gave it, and for a changing tool list says the one thing there is to do about it; the *When the prompt cache keeps missing* notification carries it once a day. The status line's own bar shows the session's miss share ("cache 14% miss", orange from one in ten, red from one in four) from the payload, or today's from the app's report when the payload has none. Pinned by `PromptCacheTests` and `StatuslineTests`.

## What a session's row says of its turn

The Sessions card shows no window and no spend, so nothing on it carries a `WindowSource`; what it does carry are counts and states that Claude Code's hook states outright (since 0.11, [docs/hooks.md](hooks.md#the-events-011-added); the hooks reference read 2026-09-24), and each is held to the same rule as a figure: shown only while its evidence stands, never estimated. Pinned by `HookEventTracking`, `HookEventStore` and `HookEventViews` in `HookEventsTests.swift`.

- **The context fill** is the session's own status line's `context_window.used_percentage` and nothing else. A `PostCompact` reported after that status line retires it, on the row and on the Claude card's arc alike (`UsageStore.contextUsed`), because the figure described the conversation before its summary; the gauge comes back with the next status line, which Claude Code sends after the turn. Between the two there is no figure, rather than a guess at what compaction left.
- **Compactions** are the `PostCompact` events the app heard while it ran: a count of those, and the trigger and time of the last. A session older than the app's launch may have compacted before, so the count is "compacted N times while the app watched", in those words. A `PreCompact` with no `PostCompact` stops saying *Compacting* at the turn's end, or after ten minutes.
- **The model** is whichever of two Claude Code sources spoke last: the status line's `model.display_name`, or a `PostModelSwitch`'s `to_model` put into the same form by a fixed rule (`Hook.modelDisplayName`: the family and its version from a Claude model id, `claude-3-5-sonnet-20241022` → *Sonnet 3.5*, dates, context suffixes, provider prefixes and version tails dropped; an id naming no Claude family shown as it came). The record of switches is the `PostModelSwitch` events heard, the last eight, a switch to the model already running (a resume) left out; *fell back by itself* is `source: auto` and nothing inferred. The chip is drawn only on a row that has heard a switch or when the rows' models differ (SessionsCard.rows): a figure the same on every row tells them apart by nothing, so it is not drawn rather than drawn as noise.
- **May be stuck** is a flag, not a figure, and its rule is written down here because it is the one judgement on the card: at least five tool calls (`SessionTracker.stuckAfter`) have failed in a row (`PostToolUseFailure`, aborts not counted) with none succeeding between them, where a batch (`PostToolBatch`) in which not every call failed ends the run, per agent; the session is mid-turn and reports batches; and the last failure is under ten minutes old. A batch whose size the command could not read counts as progress, and a failure whose payload the command read only to its head (`truncated`: its `is_interrupt` and `agent_id` sit past the cut in Claude Code's payload) counts towards nothing, so the flag errs towards silence both ways. It says *may* because a stubborn failure being worked through looks the same from here as a loop.
- **Refusals** are the `PermissionDenied` events of the current turn, at most twenty, each with the tool's name and the kind of refusal told from the shape of the reason, never its words.
- **Idle teammates** are the teammates `TeammateIdle` named since the session's last prompt, one entry each, dropped after thirty minutes with nothing heard, on the row of the session whose id the payload carries. Which session that is has not been verified against a live team (2026-09-24): the hooks reference does not say, and the agent-teams page calls each teammate "a full, independent Claude Code session", so the id may well be the teammate's own rather than the lead's, in which case the chip marks the teammate's own row and rolls nothing up onto the lead. Claude Code reports a teammate going idle and not waking again, so the list can outlast a teammate's idleness until that session's next prompt clears it; the chip says *idle* and its time, which is what was reported.
- **An MCP server's wait** is the `Elicitation` event (or, from an install without it, the dialog's `elicitation_dialog` notification), and it ends with `ElicitationResult`, the one event that says a wait was answered.

## The weekly-cap boundary

On 2026-09-14 Anthropic changed what the weekly window measures while its used percentage went on reading the same, so a row of the drain log from before that moment and one from after describe different quantities. The log holds this as a row of its own, `kind: "boundary"` (time, tool, window, a note; never a figure), written once for Claude's `seven_day` at 2026-09-14T00:00Z on the first launch after the upgrade, whether or not the app ran on the day, and idempotent, so every launch may ask. The utilization parse never sees a boundary row, and every compaction re-emits it unconditionally, as the extra-usage rows are, since the rows either side of it are the ones it explains. Its one effect on a figure: the session-metering median above is floored at the newest Claude boundary (`ClaudeCostScanner.metering(since:)`), because a day that began before the change is no norm for the days after it and the 30-day median would otherwise have carried the old figures into mid-October. The seven-day rate percentiles behind the run-out interval need no floor: the log keeps seven days, so they had healed by 2026-09-21. `--probe` lists the boundaries on file. Pinned by `DrainLogTests` and `RunOutTests`.

## The budget

A budget is one number over every tool, so the spend it is measured against is the total across the rows, not Claude Code's alone. Where more than one tool is spending, the advice names whichever is most of it — "At this rate the month costs $310 against a $200 budget. Claude Code is $240 of it." — because that is where a cut would come from.

A monthly or weekly budget (Settings › Appearance › Usage display, in the currency shown) is treated as one more window: the month's spend over the budget is the used fraction, the calendar month (or the week from its start) is the period, the same pace tick applies, and the on-track, behind and run-out notifications fire for it with the month as their once-per-period memory. Its `source` is `localEstimate`, because both sides of the fraction are this Mac's arithmetic. The extra-usage notice ("Extra usage rose $4.20 in 1h while your plan has 87% left") reads the vendor's own extra-usage figure from the usage endpoint and says it once a month, louder while the plan still has room.

## Sessions found without the hook

The Sessions card lists sessions the hook never reported (0.9.0; [docs/hooks.md](hooks.md#sessions-found-without-the-hook) has what is read and how the hook takes a row over). Each such session carries `source: detected` (`AgentSession.Source`), in the report as on the card, where the row wears a *detected* mark whose help says what follows. A session only Claude Code's status line has reported carries `source: statusline` and wears the same mark: the status line's figures (the model, the name, the context fill, the line counts) are Claude Code's own and stand, but the status line says nothing of a turn's start or end, so its working or idle is the scan's guess by the rules below, and until the hook's first event the row is a detected one in everything but those figures. The Welcome flow installs the status line even when the hook is declined, so this is the common half-set-up session; before this rule it sat idle with no mark and was never offered the hook. Everything on such a row is either read from a file or a process, or a guess stated as one:

| On the row | Where it comes from | Kind |
|---|---|---|
| That the session exists | a process of this user's in a terminal, named like an assistant (`SessionDetection.tool`) | read |
| Project and branch | the process's working directory, reduced as the hook reduces it; `.git/HEAD` | read |
| Claude Code's session id, start, busy or idle | `~/.claude/sessions/<pid>.json`: `sessionId`, `startedAt`, `status`, `statusUpdatedAt`. The file's existence is documented ("one small file per running session", [Claude directory](https://code.claude.com/docs/en/claude-directory), read 2026-09-24); its fields are not, and were read off Claude Code 2.1.281 on 2026-09-24, so each is optional and an unknown `status` is no status | read, undocumented |
| Claude Code's model and title | the last 256 KB of the session's transcript: the newest reply's `message.model`, a `custom-title` line (`/rename`), an `ai-title` line (Claude Code's own) | read |
| Working or idle, for the other assistants, or a Claude Code whose file has no status | a transcript written in the last 30 s; else the process's CPU time since the previous scan, working above 2 % of one core (a working Claude Code measured 10 to 16 %, an idle Node process 0.0 %, 2026-09-24) | **guess** |
| How long the turn has run | Claude Code's `statusUpdatedAt` when its file says busy; otherwise the first scan that saw it working | read, or a guess to within a scan |
| How long it has been idle | the newest of the status change, the transcript's last write and the last scan that saw it busy; the process's start when none | read, or a guess to within a scan |

**What detection cannot know.** Whether an assistant is holding a prompt open for you: a process at a permission prompt is still drawing its spinner, so the row says *working* through it, and a detected session never shows the waiting hand, never raises a waiting notification or news in the notch, and never answers anything. The exact end of a turn: it lands on the next scan or two, a few seconds late, and it lights no *just finished* tick and sends no *turn finished* notification, because a guess at a moment is not the moment. And a detected session's working never holds the Mac awake. The hook reports all of these exactly, which is what the card's upgrade line says; the moment it reports a session, the row is the hook's.

**What it never does.** It estimates no figure: the scan puts no context fill on a row (one comes only from Claude Code's own status line, when that reports the same session), no subagents and no task list; and it does not turn a session count into a fact the calm rule may act on: until an assistant's hook has reported, that assistant's count stays unknown whatever the scan finds (`SessionTracker.knownCount`). With *Show what a session is working on* off, or the screen shared, the two title lines of a transcript are not parsed at all and the session file's `name` is not read, and every title the scan had read before goes from its cache the moment the setting turns off (`SessionDetector.allowTitles`): nothing of a prompt is held anywhere, not only nothing new.

**What the scan leaves alone.** A session the hook has reported, by id, however much more the scan knows about it. An id the hook ended inside the last 15 s (`SessionTracker.endedGrace`): Claude Code runs `SessionEnd` on its way out, while its process is still there and before its `sessions/<pid>.json` goes ("removed when its session exits", the [Claude directory](https://code.claude.com/docs/en/claude-directory) page, read 2026-09-24), so a scan in that window would put a fresh detected row where the ended one had been for one scan; a session file started after the end is a new session under a reused id (`claude --resume`) and is taken whatever the clock says. And, for a process the scan cannot name (every assistant but Claude Code), one process of its assistant and project for each hook session on this Mac that is live, or set aside while working or inside the 30 minutes an idle row stays on the list (`idleAfter`): a hook session set aside longer than that accounts for nothing, because Cursor and Codex send no end when a chat is closed, and a dead session that still counted hid a live process of the same assistant and project for the four hours `staleAfter` keeps it. The project name is resolved afresh on every scan (a folder becomes a checkout or a worktree while its assistant runs), as the branch is. Pinned by `SessionDetectionTests` and `DetectedSessionsTests`.

## Codex sessions

Codex writes one JSONL rollout per session under `$CODEX_HOME/sessions` (`~/.codex/sessions`, in dated folders). Every line is a `RolloutLine`: a `timestamp`, a `type` and a `payload`. Four types are read and the rest of the file — the conversation itself — is never parsed. The shapes below were read from the codex-rs source on 2026-09-02, at [`codex-rs/protocol/src/protocol.rs`](https://raw.githubusercontent.com/openai/codex/main/codex-rs/protocol/src/protocol.rs), [`codex-rs/history/src/rollout_payload.rs`](https://raw.githubusercontent.com/openai/codex/main/codex-rs/history/src/rollout_payload.rs) and [`codex-rs/history/src/lib.rs`](https://raw.githubusercontent.com/openai/codex/main/codex-rs/history/src/lib.rs):

| Line `type` | What is taken | Rust type |
|---|---|---|
| `turn_context` | `payload.model` (the model the turn ran on) and `payload.cwd` reduced to its last path component (the project) | `TurnContextItem` |
| `session_meta` | `payload.cwd`, as the project until a `turn_context` names one | `SessionMetaLine` |
| `token_usage_record` | `payload.usage` (the turn's own usage) and `payload.response_id` | `TokenUsageRecord` |
| `event_msg` with `payload.type == "token_count"` | `payload.info.last_token_usage` | `TokenCountEvent` → `TokenUsageInfo` |

A `TokenUsage` carries `input_tokens`, `cached_input_tokens`, `cache_write_input_tokens`, `output_tokens`, `reasoning_output_tokens` and `total_tokens`. Two of those relationships decide the arithmetic, and both are the source's own:

- **`input_tokens` includes the cached tokens.** codex-rs computes `non_cached_input()` as `input_tokens - cached_input()`. So the billed fresh input is `input_tokens − cached_input_tokens`, and the cached part is priced at the cached rate. Counting `input_tokens` whole would charge the cached tokens twice.
- **`reasoning_output_tokens` is inside `output_tokens`.** codex-rs prints `output={output_tokens} (reasoning {reasoning_output_tokens})` and its `blended_total()` is `non_cached_input + output_tokens`, with reasoning never added on top. So output is taken whole and reasoning is not added again.
- **`cache_write_input_tokens` is carried into the cache-write bucket.** Whether writing the cache costs anything depends on the model, and the rule is under *Cache writes* below.

`token_usage_record` is the per-response record and is what a current build writes. Where a file has any, the `token_count` events in the same file are ignored, because they describe the same turns; a file that has only events (an older build) is priced from each event's `last_token_usage`, which is the turn that just finished, so the events sum to the session rather than to a running total counted repeatedly. Resuming a thread replays the turns it inherited under the same `response_id`, so one entry per response id is kept and the first wins.

**The rates.** OpenAI list prices per million tokens, read on **2026-09-02** from [developers.openai.com/api/docs/pricing](https://developers.openai.com/api/docs/pricing) (to which platform.openai.com/docs/pricing now redirects), from the per-model page under `developers.openai.com/api/docs/models/<id>` for the Codex ids that page does not list, and from the [prompt-caching guide](https://developers.openai.com/api/docs/guides/prompt-caching) for the cache-write rule. These are the **standard** tier: Batch, Flex, Priority and Fast mode are separate published tables, and a rollout does not record which one a turn ran on. `OpenAIPricing.fingerprint` is the snapshot date *and* a digest of the rows themselves, and it is part of the cache key, so day records priced under other rates are never reused — including after a rate is corrected on the same day the snapshot was taken.

| Model | Input | Cached input | Cache write | Output | >272K tier |
|---|---|---|---|---|---|
| gpt-5.6-sol | $4.00 | $0.40 | $5.00 | $20.00 | yes |
| gpt-5.6-terra | $2.00 | $0.20 | $2.50 | $12.00 | yes |
| gpt-5.6-luna | $0.20 | $0.02 | $0.25 | $1.20 | yes |
| gpt-5.6-cyber | $12.50 | $1.25 | $15.625 | $75.00 | yes |
| gpt-5.5-cyber | $12.50 | $1.25 | not billed | $75.00 | — |
| gpt-5.5 | $5.00 | $0.50 | not billed | $30.00 | yes |
| gpt-5.5-pro | $30.00 | — | not billed | $180.00 | yes |
| gpt-5.4 | $2.50 | $0.25 | not billed | $15.00 | yes |
| gpt-5.4-mini | $0.75 | $0.075 | not billed | $4.50 | — |
| gpt-5.4-nano | $0.20 | $0.02 | not billed | $1.25 | — |
| gpt-5.4-pro | $30.00 | — | not billed | $180.00 | yes |
| gpt-5.3-codex | $1.75 | $0.175 | not billed | $14.00 | — |
| gpt-5.2, gpt-5.2-codex | $1.75 | $0.175 | not billed | $14.00 | — |
| gpt-5.2-pro | $21.00 | — | not billed | $168.00 | — |
| gpt-5.1, gpt-5.1-codex | $1.25 | $0.125 | not billed | $10.00 | — |
| gpt-5.1-codex-mini | $0.25 | $0.025 | not billed | $2.00 | — |
| gpt-5, gpt-5-codex | $1.25 | $0.125 | not billed | $10.00 | — |
| gpt-5-mini | $0.25 | $0.025 | not billed | $2.00 | — |
| gpt-5-nano | $0.05 | $0.005 | not billed | $0.40 | — |
| gpt-5-pro | $15.00 | — | not billed | $120.00 | — |
| gpt-4.1 | $2.00 | $0.50 | not billed | $8.00 | — |
| gpt-4.1-mini | $0.40 | $0.10 | not billed | $1.60 | — |
| gpt-4.1-nano | $0.10 | $0.025 | not billed | $0.40 | — |
| o3 | $2.00 | $0.50 | not billed | $8.00 | — |
| o3-pro | $20.00 | — | not billed | $80.00 | — |
| o3-mini | $1.10 | $0.55 | not billed | $4.40 | — |
| o4-mini | $1.10 | $0.275 | not billed | $4.40 | — |

**How an id finds its row.** By **exact match on the id**, never by prefix. A `provider/model` id is reduced to the part after the slash and the id is lower-cased first, and a trailing date snapshot is stripped (`gpt-5-mini-2025-08-07` → `gpt-5-mini`) because the page lists a dated id and the id it snapshots at the same price. Nothing else is rewritten, so an id the table does not hold cannot land on a shorter row that happens to be a prefix of it: `gpt-5.6-nova` is not priced as `gpt-5.6`, and `gpt-5.4-cyber` is not priced as `gpt-5.4`. Until 2026-09-02 the lookup was a prefix scan in table order and did exactly that, which is why `unlistedIdsAreNamedRatherThanCollapsed` now pins every id in the table resolving to its own row and a set of unlisted family members resolving to none. Where the page prices no cached rate (the `pro` tiers, which do not serve the prompt cache) the input rate is used for cached tokens too, so a cached count that should never arrive cannot come out cheaper than it was. Each `-codex` id carries the row **its own model page** publishes; the pricing table lists only `gpt-5.3-codex`, and nothing is inferred from the numbered model a Codex variant is built on.

**Cache writes.** The prompt-caching guide states them per family: "For GPT-5.6 and later, cache writes cost 1.25× the standard, uncached input-token rate", and "No additional cache-write charge" for "GPT-5.5 and GPT-5.5 Pro" and "Other earlier models". So the write bucket is billed on the four gpt-5.6 rows and at nothing on every other row, and each of those four published write rates is that rule's own arithmetic ($4.00 → $5.00, $2.00 → $2.50, $0.20 → $0.25, $12.50 → $15.625), which is the cross-check `cacheWritesAreBilledOnlyFromGPT56AndAt125xInput` pins.

**The long-context tier.** Above 272,000 input tokens on a model that publishes the tier, the whole request is priced at 2x every input bucket and 1.5x output. Seven rows publish their long-context rates in full (sol, terra, luna, gpt-5.5, gpt-5.5-pro, gpt-5.4, gpt-5.4-pro) and those two multipliers reproduce all seven exactly, which is what `theLongContextTierMatchesThePublishedRows` pins. gpt-5.6-cyber is the one id where the tier comes from prose rather than a row: its own model page states the >272K rule while the pricing table leaves its long-context columns empty, and the rule is applied. **gpt-5.5-cyber has neither** — no long-context row and no model page left to read — so a turn of it over 272K is priced at its published short rates, and would be under-reported if OpenAI in fact bills it the tier. No other row carries a threshold at all, so no other model can pick up a surcharge nothing published for it.

**What is not priced.** Four gaps, all visible rather than filled in:

- A turn whose rollout never named a model is counted in tokens and priced at nothing, and the card says so ("1 Codex turn(s) name no model and are not priced") rather than showing a confident $0.
- A model id the table does not hold contributes nothing and is named under "Unpriced: …". `gpt-5.4-cyber` is the live example — OpenAI publishes it with a dash in every column, so there is no rate to apply — and it is the fixture `anUnpricedModelCostsNothingAndIsNamed` uses.
- Only the standard tier is priced. A turn that ran under Batch, Flex, Priority or Fast mode is priced at the standard rate, because the rollout does not say which tier it used; Fast mode is roughly twice standard (`gpt-5.3-codex` is $3.50 / $28.00 there), so such a turn is under-reported rather than over.
- A write to the prompt cache on a row that publishes no write rate costs nothing here, which is what the caching guide says of every family before GPT-5.6.

**What this figure is not.** It is the API-equivalent value of the work the sessions did, at list price. A ChatGPT Plus, Pro or Business plan includes Codex usage in the subscription, so on those plans nothing here is a bill and the figure is a measure of pace and of what the plan is worth. On an API key it is an estimate at list price; contracted rates, Batch and Flex discounts and any credit are not modelled.

**With no sessions.** This Mac has no Codex sessions at all, so the whole path is exercised by unit tests over synthetic rollouts written in the shape above (`CodexCostTests`), and a Mac with no `sessions` folder produces no Codex row — not $0. The first live run against real rollouts is a user's.

Day totals are appended to the same daily-history file Claude Code uses, under the `codex` tool key, so a day survives Codex deleting its rollouts. As there, a day is taken from the history when the history's total is larger than what the rollouts now price to.

## Cursor's usage events

With *Also read Cursor's usage events* on (on by default; it can be switched off in Settings), a second read of cursor.com on the same session cookie (`POST /api/dashboard/get-filtered-usage-events`, the request the dashboard's usage page makes) returns the last 30 days of usage events with the cost Cursor itself assigns to each. **Nothing is priced here.** Every dollar on the Cursor row is the one Cursor put on that event, which is why the row's source reads "billing export" rather than an estimate, and why a request or a model Cursor left unpriced stays unpriced instead of being valued at somebody else's rates.

Events are folded by local day into the daily-history file under the `cursor` tool key — cost, tokens, and cost and tokens per model where the event names one — and read back as a full row on the Cost card: today, yesterday, the week, the month, 30 and 90 days, the daily trend under the Cursor card, and the per-model shares in the range's breakdown.

Two limits are worth stating. The export is **day resolution**: an event carries a day, not an hour that can be trusted for a last-hour figure, so Cursor reports no hour and takes no part in the burn line. And the export reaches back **30 days**; the 90-day range and anything older is whatever the daily-history file has accumulated since Notchmeter was installed, and is short until it has been running that long.

The fetch happens on the Cursor provider's own polling loop, and the Cost card reads only the file it writes, so a slow, refused or expired Cursor read never delays a cost scan: the row keeps its last figures, its timestamp stays at the last successful read, and the failure is printed under the row. The endpoint is undocumented, its shape was taken from the dashboard's own traffic and pinned in `CursorParsingTests`, and this Mac's free plan returns no events, so the first live run with a paid plan is a user's.

## Cursor's cycle figures and the Grok Bot window

Two more dashboard reads on the same session cookie, both fail-soft: whatever they answer, the summary's windows stand (`CursorSpendTests` pins a 403 on both leaving the reading untouched).

**The cycle figures** (`POST /api/dashboard/get-current-period-usage`, body `{}`, the CSRF `Origin` the export needs) are asked for only on a seat the summary meters nothing for — no included or team-pooled window with a figure — which is the Enterprise on-demand seat of 2026-09-18 and the reason the *Today's spend* heuristic exists. `planUsage` arrives in cents: `includedSpend` is what counts against `limit`, `bonusSpend` is provider-granted usage that is never charged to the allowance, and `totalSpend` is the two added (an Ultra seat read $642 of total against a $400 limit, which is not 160 % of anything), so the included ring is `includedSpend / limit`, or the total less the bonus where `includedSpend` is absent. When the endpoint carries a real fraction for the seat, that figure fills the `included` window and *Today's spend* yields by the gate that already existed — it leads only where no included or pooled window has a figure — so the vendor's own number displaces the heuristic without a second rule, and the heuristic stays for seats nothing meters (`CursorSpendTests.aSeatTheCycleEndpointMetersLeadsWithTheVendorsFigureAndTodaysSpendYields`). A summary figure that already reads further along is kept, and one that reads lower takes the cycle's, by the same furthest-along rule as above. The three percentage fields are not read as spend fractions (*Known divergences*, 6). The cycle bounds arrive as epoch milliseconds, as a number or a string, and either is read.

**The Grok Bot window** (`POST /api/dashboard/get-sand-usage-status`, `{}`) is a seven-day allowance a seat may or may not have: it becomes a window only where `includedLimitZero` is false (the older `hasNonZeroIncludedLimit` where the new flag is absent) or a `sandTrialExpiresAt` is still ahead; `usagePercent` is the figure, `nextResetTimestampUtc` the reset, and the length is the reset less `currentPeriodStart`. A trial has no recurring reset and no length, and is captioned as a trial. An expired trial, a seat without the allowance, or anything unreadable is no window at all, never an empty bar.

## GitHub Copilot's AI credits

The Copilot row is *Which tools report spend* above, in full: the count is GitHub's (`credits_used`, a number or a string, summed across the snapshots), the rate is GitHub's (a cent a credit), and the day is this Mac's observation. Three post-June shapes are pinned in `CopilotParsingTests`: a metered seat with an entitlement and a remainder, where `unlimited: true` or a `-1` in either count means no limit; an org-managed token-billed seat whose snapshots are `entitlement: 0, remaining: 0` placeholders beside a live `credits_used`, where the placeholder is never drawn as a 0 % (or, under `percent_remaining: 100`, a full) bar and only the count survives; and a free individual whose premium snapshot is the same placeholder under `percent_remaining: 0`. Without `percent_remaining` the fraction is `remaining / entitlement`; `quota_reset_date` is read as `yyyy-MM-dd` or an ISO instant, and `quota_reset_date_utc` and the free tier's `limited_user_reset_date` are read too. On a token-billed seat the premium counts are credits, so the window is named *AI credits* and carries its dollars; on any other snapshot a count is a count. The endpoint is undocumented and keyed on the client identity it answers to, so the request carries the editor, plugin and API-version headers the two live-validated trackers send (`Editor-Version: vscode/1.96.2`, `Editor-Plugin-Version: copilot-chat/0.26.7`, `User-Agent: GitHubCopilotChat/0.26.7`, `X-Github-Api-Version: 2025-04-01`); the organisation billing endpoints are public API and keep the app's own user agent and the documented `2022-11-28` version. `CostEngineTests` pins that `ToolID.reportsCost` is true for exactly Claude Code, Codex, Cursor, Copilot and OpenCode, so a future tool cannot quietly acquire a made-up figure.

## Gemini CLI and Antigravity: quota, and no cost

Neither meters money: the Code Assist endpoints return per-model or per-group windows and the fraction of each that is left, no price and no token count a published rate could be applied to, and neither seat is billed at Gemini's API rates. There is nothing to derive a dollar figure from, so the Cost card has no row for either.

**Two rows since 0.9.0.** Until then Gemini CLI and Antigravity were one *Antigravity* row, read under whichever identity the Mac's installs suggested and lit by Gemini CLI's hook. They are two rows now, because the backend picks the licence it checks from the caller's identity, so the two products can hold different quotas on one Google account and one row could only ever show one of them (`CodeAssistProvider`, one actor type with a `tool` of `.gemini` or `.antigravity`):

| | Gemini CLI row | Antigravity row |
|---|---|---|
| Shown when | Gemini CLI's login is on this Mac (`~/.gemini/oauth_creds.json`) | the app, its home folder or its CLI's folder is (`/Applications/Antigravity.app`, `~/.antigravity`, `~/.gemini/antigravity-cli`) |
| Login read | `~/.gemini/oauth_creds.json` | the same file: the app and `agy` keep their own login in the Keychain, so the row says to sign in to Gemini CLI with the same account when it is missing |
| Identity sent | Gemini CLI's (`ideType: GEMINI_CLI`, `User-Agent: Notchmeter/<version>`) | Antigravity's (`ideType: ANTIGRAVITY`, `User-Agent: antigravity`, `Client-Metadata`) |
| Hosts asked | both, daily first, the first with a live figure believed | the one Antigravity's CLI logged, else both |
| Staleness guard's activity | Gemini CLI's hook, and its `tmp` folders and login file | Antigravity's conversation folders |
| Hook | Gemini CLI's (`--hook --tool gemini`) | none: the IDE's hooks report nothing the notch can act on |

A Mac with only Gemini CLI shows only the Gemini CLI row; one with both shows both, and the two figures can rightly differ. Settings made on the combined row carry over to the Gemini CLI row once (`ToolMigration`: on or off, its place just before Antigravity, the menu bar pin, peak hours, the ring and Hide choices), and a Gemini CLI hook entry still on the old `--tool antigravity` flag is read as Gemini CLI's and reads as out of date until Repair, or the launch repair, rewrites it. The drain log is kept per row, so the Gemini CLI row starts its own history on the first read after the update, and a length inferred from resets (below) is confirmed afresh.

The quota itself is read under rules that changed in September 2026, each pinned in `CodeAssistParsingTests`:

- **Identity.** A licence is held by the user, not a project: the project-scoped `retrieveUserQuota` answers 403 *"You do not have a valid license of this product"* for an Antigravity-only account while the same call with `{}` answers, and the backend picks which product's licence to check from the caller's identity. The Antigravity row's calls carry `User-Agent: antigravity`, a `Client-Metadata` naming the IDE, and `ideType: ANTIGRAVITY` in `loadCodeAssist`; the Gemini CLI row keeps Gemini CLI's own identity. A 403 to a project-scoped body is retried with `{}` before it counts as a refusal, and the first refusal's body is what the shutdown diagnosis reads (`SUBSCRIPTION_REQUIRED`).
- **The summary first.** `retrieveUserQuotaSummary` (`{}`) is the answer Antigravity's own panel shows — groups (*Gemini models*, *Claude and GPT models*) with a session, weekly or daily bucket each, the window length declared — so it is asked first and needs no inferred period; the per-model buckets are the fallback. A remaining fraction is read from `remainingFraction` or from the proto-JSON `remaining.remainingFraction` / `remaining.value` forms.
- **Two hosts.** The quota is metered on one of two deployments, `cloudcode-pa.googleapis.com` and `daily-cloudcode-pa.googleapis.com`, and the other answers every bucket untouched with a reset five hours from the moment it was asked, whatever has been used (antigravity-cli #387, token-monitor #722, graviton #347). The Antigravity row prefers the host Antigravity's CLI last logged in `~/.gemini/antigravity-cli/cli.log`; without a log, and always for the Gemini CLI row, whose calls that log says nothing about, both are tried, daily first, and the first answer with a live figure — something used, or a reset that is not the placeholder — is believed. Only after both hosts fail is the June 2026 shutdown reported, and it is reported as a calm state rather than a fault (`ProviderError.notServed`, 2026-09-24): Google's answer is documented as permanent, so the row reads idle with the sentence as its note, hides under *Hide assistants with nothing to show*, and is asked again about hourly (`UsageStore.notServedBackoff`) rather than every five minutes. That is where a personal Google AI Pro account, Antigravity's own user base, lands on the Gemini CLI row after the split, while its Antigravity row reads as before (`ToolSplitTests`).
- **What a bucket means.** An absent `remainingFraction` is unknown: the window keeps its reset and draws no figure, never 100 % left. An explicit 0 is exhausted. A payload whose every figured bucket reads untouched at one identical reset is the unmetering host's answer and is shown with no figure and a caption saying so, not as an untouched quota; one untouched window alone is left as read.
- **The staleness guard.** One window pinned at untouched across three consecutive polls, while the row's own tool was seen at work after the run began, loses its figure, is tagged *inferred*, and says it is unverified (`CodeAssistStaleness`, counted per row). The test is by history, never by value, so a first read cannot fire it and a quiet Mac cannot either; a figure that moves ends the run, and the counts live in memory only, so a relaunch starts them again.

Google declares no window length for the per-model buckets, so those windows' lengths are still inferred from consecutive resets and tagged as such (`InferredPeriods`, *inferred*); the summary's windows declare theirs and are not inferred over.

## Kimi Code: an allowance, two shapes of answer

Rule, source and date. The figures are Moonshot's own, read the way Kimi Code's own CLI reads them for its `/usage` command: `GET https://api.kimi.com/coding/v1/usages` (the CLI's `_usage_url` and `_fetch_usage` in `src/kimi_cli/ui/shell/usage.py`; the base from `_kimi_code_base_url` in `src/kimi_cli/auth/platforms.py`, `KIMI_CODE_BASE_URL` overriding it), with the CLI's OAuth access token as `Authorization: Bearer`, from `credentials/kimi-code.json` in its share folder (`$KIMI_SHARE_DIR`, else `~/.kimi`; `src/kimi_cli/auth/oauth.py` writes it 0600 with `expires_at` in epoch seconds). MoonshotAI/kimi-cli at `main`, read 2026-09-24. The endpoint is not published as an API; it is the request Moonshot's own client makes, and it can change without notice. An override of the base is honoured only over https and only on Moonshot's own hosts (`kimi.com`, `kimi.ai`, `moonshot.cn`, `moonshot.ai`), so the token cannot be sent anywhere else. Pinned in `KimiTests`.

**The token is read, never refreshed.** The CLI refreshes its short-lived access token while it runs and rotates the refresh token as it does; a refresh from here would sign the CLI out. So a token within thirty seconds of its `expires_at` is not sent: the card says the login has expired and to run Kimi Code once, and the last reading stays on screen, captioned as possibly out of date, until the CLI has renewed it. An `expires_at` of 0 is an expiry the CLI was never told, and is treated as unknown rather than long past.

**Two shapes.** The answer has been seen in two shapes, and both are read:

- *Counts*, what the CLI's own parser reads: `usage`, the summary row (`limit`, and `used` or `remaining`, where used is `limit − remaining` as the CLI computes it, and a reset as `reset_at`, `resetAt`, `reset_time` or `resetTime`, or as seconds to go in `reset_in`, `resetIn` or `ttl`), and `limits[]`, each a `window` of `duration` and `timeUnit` (`TIME_UNIT_MINUTE`, `_HOUR`, `_DAY`, `_WEEK`) with its counts in `detail`. Every number arrives as a string and every reset in nanoseconds, which are read to the millisecond. An entry's `name` or `title`, where it has one, is kept as the vendor's words.
- *Ratio pools*: `usages`, a dictionary of `limit_5h`, `limit_7d`, `limit_month_total` and `limit_month_code`, each a `used_ratio` from 0 to 1 and a `reset_time`, beside or in place of the counts (steipete/CodexBar #3694, 2026-09-16). The two monthly pools are windows of their own, *Monthly total* and *Monthly coding*; any other `limit_<n><unit>` key is read by the length it names. An answer wrapped in `data` reads the same.

**Window lengths.** A `limits[]` entry and a pool key declare their length. The summary row declares none; it is the week, because the CLI labels it "Weekly limit" and Kimi's help centre says the Kimi Code allowance refreshes every 7 days from the subscription date. An entry with no length and no name is numbered, as the CLI numbers it (*Limit 1*), and has its length inferred from its resets like a Google bucket's (`InferredPeriods`, *inferred*). A unit this parser does not know gives no length rather than a guess at seconds.

**Where the two shapes disagree.** On 2026-09-20 one account read `usage.used: 100` of `limit: 100` beside `usages.limit_7d.used_ratio: 0` for the same week, while the API refused every request with 403 (MoonshotAI/kimi-code #3951); a second report has zero ratios beside non-zero counts (steipete/CodexBar #3754). The five-hour window and the week can each be described twice, once by counts and once by a ratio, and **the window is as spent as its furthest-along figure says**, the rule Cursor's two figures already follow (*When a vendor publishes two figures for one window*, above): under-reporting a spent allowance hides the reason requests are being refused. The counts' reset is the one kept, since it is the one the CLI prints; the remaining count is printed under the bar ("61 of 200 left") whenever counts were sent, so a disagreement stays visible. The same rule holds between two sources of counts: the summary row and a seven-day `limits[]` entry both describe the week, and where they disagree the one that says more is used is kept whole, its reset with it (a reset is taken from the other only when the winner sent none; on a tie, the one carrying a reset, else the summary's).

**What is not assumed.** Moonshot publishes no absolute plan sizes (its tiers are described only relative to one another), so a window with no `limit` and no ratio has no figure — not 0 %, not 100 % — and a `limit` of 0 is no limit to divide by. The answer names no plan, and none is shown. Newer Kimi Code memberships have no weekly quota, only the rolling five-hour window; for them the summary row simply is not sent. A 404 is the endpoint's answer to an account that is not a Kimi Code membership, and the card says so. A `config.toml` with no `credentials/kimi-code.json` beside it is kimi-cli on a Moonshot API-key platform (`auth/platforms.py`), which has no membership to meter: the row is calm (idle, and hidden by *Hide assistants with nothing to show*) rather than marked as needing a sign-in, and its note says how to read a membership if there is one; a login file that is there but unreadable is still a login to renew.

**Sessions.** Kimi Code's optional hook (docs/hooks.md, *Kimi Code*) reports session starts and ends, prompts, the end of each turn, a failed turn, and subagents. It has no permission event, and its `Notification` is a background-task notice rather than a wait on the user, so a Kimi session is never shown as waiting on you, and a failed turn, whose `error_type` is a Python exception's class name rather than a documented kind, never claims a rate limit. Without the hook, `~/.kimi/sessions` sets the polling cadence from its newest session files' modification times, and nothing else is read from them.

## OpenCode: its own records, and a Go meter computed here

OpenCode keeps everything on this Mac in its own database, and Notchmeter reads it and nothing else: there is no network request for OpenCode at all, no login is read (OpenCode's `auth.json` is never opened), and nothing is written. Three things come from it: OpenCode's row on the Cost card, the OpenCode Go meter, and OpenCode's sessions when its plugin is not installed. The sources below were read on 2026-09-24 from anomalyco/opencode's `dev` branch; OpenCode's storage has changed shape three times in 2026, so every one of them is read and none is assumed.

**Where it is.** OpenCode's data folder is `$XDG_DATA_HOME/opencode`, else `~/.local/share/opencode` (`packages/core/src/global.ts`). The database is `$OPENCODE_DB` when set, else `opencode.db` for a release build and `opencode-<channel>.db` for any other channel (`packages/core/src/database/database.ts`); a channel switch has left such a file behind unmigrated (anomalyco/opencode#21790), so every `opencode*.db` in the folder is read, `opencode.db` first. Builds before 1.2 kept one JSON file per message under `storage/message/<session>/<message>.json` (`packages/opencode/src/storage/storage.ts`), and files there modified inside the window are read too. Only the environment the app itself was started with is consulted: a Finder launch that inherited no `XDG_*` looks in the default folders.

**What is read, and what never is.** Three tables: `message` (from 1.2, one JSON object per message: `role`, `providerID`, `modelID`, `time.created` and `time.completed` in milliseconds, `path.cwd`, `cost`, `tokens`, `finish`, `error`), `session_message` (the event-sourced session store added in 2026, where an `assistant` row carries `model {id, providerID}` and an optional `cost` and `tokens`), and `session` (`directory`, `parent_id`, `title`, `time_updated`, `time_archived`). The same file holds `part` (the text of every prompt and reply and every tool's output), `account`, `control_account` and `credential` (tokens), and `session_input` (queued prompts); none of them is ever queried. Only a user or assistant message's own record is parsed, never its parts. The file is opened with SQLite's read-only flag and `PRAGMA query_only`, as `mode=ro` while OpenCode's write-ahead log exists (sharing it like any reader) and as `immutable=1` while it does not (so the read neither locks nor creates a sidecar); it is never copied and never written. A turn met twice (the same message in a left-behind channel database, or in legacy files a later build imported) is counted once, by message id, and a session whose turns the `message` table holds is not read again from `session_message`, which would count them twice.

**The buckets.** OpenCode's own `getUsage` (`packages/opencode/src/session/session.ts`) stores `input` net of both cache buckets, `cache.read` and `cache.write` as the cache, and `output` net of `reasoning`, which it charges at the output rate. So the input bucket here is `input`, the cache-read bucket `cache.read`, the (5-minute) cache-write bucket `cache.write`, and the output bucket `output + reasoning`. The context a long-context price row is decided on is what the provider was sent, `input + cache.read + cache.write`, which is OpenCode's own `contextTokens`. A message with no tokens and no cost yet is a turn still streaming and is skipped until it has them.

**Which figure a turn is worth** (`OpenCodePricing.price`, in this order):

1. `providerID` `opencode-go`: the Go page's rates below, for a model the page lists. A Go model it does not list falls to rule 2.
2. The `cost` OpenCode recorded, when it is above zero. On OpenCode Zen (`providerID` `opencode`, OpenCode's own pay-as-you-go gateway) the recorded cost is taken even at zero, since Zen's free models are free.
3. A recorded zero on `anthropic` or `openai`: that vendor's list rate from the tables above (`ModelPricing`, `OpenAIPricing`), with their own rules for unknown ids. OpenCode records zero for a turn run on a subscription login, and the value shown is the API-equivalent value of the work, as for Claude Code.
4. Anything else: its tokens count, its dollars do not, and its model is named under *Unpriced* on the card.

Day totals go into the same daily-history file as the other tools, under the `opencode` key, and a day is taken from the history when its total is larger than what the database now prices to, so a session OpenCode deletes does not take its spend with it. The row has an hour and a burn multiple, since every turn carries its time.

### OpenCode Go

[opencode.ai/docs/go](https://opencode.ai/docs/go/), read 2026-09-24 (the page says "Last updated: Sep 24, 2026"): Go is $10 a month. "Usage limits are defined as monthly dollar amounts", set per model, and "Each model has the following usage limits: 5-hour — 20% of the monthly limit; weekly — 50%; and monthly — 100%." The page also says "Usage limits may change as we learn from early usage and feedback", which is why `GoPlan.snapshotDate` travels with every figure and is part of the cost cache's key. Prices are per million tokens; a dash in the cached-write column is priced at nothing, as a dash is in OpenAI's table.

| Model id | Input | Output | Cached read | Cached write | Monthly limit |
|---|---|---|---|---|---|
| glm-5.3-flash | $0.15 | $0.50 | $0.03 | — | $60 |
| glm-5.3 | $1.40 | $4.40 | $0.26 | — | $15 |
| glm-5.2, glm-5.1 | $1.40 | $4.40 | $0.26 | — | $60 |
| kimi-k3 | $3.00 | $15.00 | $0.30 | — | $15 |
| kimi-k2.7-code | $0.95 | $4.00 | $0.19 | — | $60 |
| kimi-k2.6 | $0.95 | $4.00 | $0.16 | — | $60 |
| longcat-2.0 | $0.30 | $1.20 | $0.006 | — | $60 |
| mimo-v2.6-flash, mimo-v2.5 | $0.14 | $0.28 | $0.0028 | — | $60 |
| mimo-v2.6-pro, mimo-v2.5-pro | $0.435 | $0.87 | $0.003625 | — | $15 |
| minimax-m3 | $0.30 | $1.20 | $0.06 | — | $60 |
| minimax-m2.7, minimax-m2.5 | $0.30 | $1.20 | $0.06 | $0.375 | $60 |
| muse-spark-1.3-contributor, muse-spark-1.2-contributor | $0.10 | $0.20 | $0.002 | — | $60 |
| qwen3.8-max | $2.00 | $6.00 | $0.25 | $2.50 | $15 |
| qwen3.8-flash | $0.15 | $0.47 | $0.016 | $0.20 | $30 |
| qwen3.7-max | $2.50 | $7.50 | $0.50 | $3.125 | $30 |
| qwen3.7-plus (≤ 256K / > 256K) | $0.40 / $1.20 | $1.60 / $4.80 | $0.04 / $0.12 | $0.50 / $1.50 | $60 |
| qwen3.6-plus (≤ 256K / > 256K) | $0.50 / $2.00 | $3.00 / $6.00 | $0.05 / $0.20 | $0.625 / $2.50 | $60 |
| deepseek-v4.1-flash (off-peak / peak) | $0.15 / $0.30 | $0.60 / $1.20 | $0.003 / $0.006 | — | $15 ($60 until Sep 27) |
| deepseek-v4-pro (off-peak / peak) | $0.66 / $1.32 | $1.98 / $3.96 | $0.022 / $0.044 | — | $15 |
| deepseek-v4-flash (off-peak / peak) | $0.15 / $0.30 | $0.60 / $1.20 | $0.003 / $0.006 | — | $30 |
| deepseek-v4-flash-vision-exp (off-peak / peak) | $0.15 / $0.30 | $0.60 / $1.20 | $0.003 / $0.006 | — | $15 |
| hy4-preview | $0.834 | $2.501 | $0.042 | — | $30 |
| hy3 | $0.14 | $0.58 | $0.035 | — | $60 |
| space-bunny-free | free | free | free | — | unlimited |
| grok-4.7, grok-4.6 (≤ 200K / > 200K) | $2.00 / $4.00 | $6.00 / $12.00 | $0.50 / $1.00 | — | $15 |
| gpt-6-luna (≤ 272K / > 272K) | $0.10 / $0.20 | $0.50 / $0.75 | $0.01 / $0.02 | $0.125 / $0.25 | $15 |
| gpt-5.6-luna (≤ 272K / > 272K) | $0.20 / $0.40 | $1.20 / $1.80 | $0.02 / $0.04 | $0.25 / $0.50 | $15 |

The ids are the page's own endpoint table's, which are the `modelID` OpenCode records under `opencode-go` (the page's config format is `opencode-go/<model-id>`); a lookup is exact, never by prefix, so an id the table does not hold is not priced as a neighbour. Three rules from the page's notes: DeepSeek's peak hours are "01:00-04:00 and 06:00-10:00 UTC, Monday through Friday; all other hours, including weekends, are Off-Peak", read here as each span including its first minute and not its last and applied at the turn's own time; a long-context row applies past its printed threshold, on the context the turn sent; and DeepSeek V4.1 Flash's limit is printed as a struck-through $15 beside $60 with "4x · Ends Sep 27", read as $60 until the end of 2026-09-27 in UTC (the latest reading of a date with no time or zone) and $15 from then.

**The meter** (`GoMeter`, source `computedLocally`, tagged *computed here*). Go publishes no reading of these limits an app can take: the page sends the reader to the web console. So each limit is metered on this Mac: every Go turn in the window is priced as above and summed per model, and each model's sum is set against its own share of its own monthly limit (20 %, 50 %, 100 %). The window each limit is set against is trailing: the last 5 hours, 7 days and 31 days, and each is named on the card by that length (*5-hour*, *7-day*, *31-day*), the way every window the app cannot anchor is named, rather than as Go's *weekly* and *monthly*, since a calendar month is not what the figure covers. The page does not say when its windows start (the one client that reads Go's own figures calls the 5-hour window `rolling`), and a trailing window of a limit's length, or a day longer for the month, contains whichever window Go is actually counting, so the figure can read high but never low; the *computed here* tag's tooltip and VoiceOver text say so, since that is the one systematic difference between this figure and Go's own console. For the same reason no reset is claimed and no pace tick is drawn: there is no anchor to draw one from. Settings › Assistants gives the row *Go · computed from this Mac's turns* rather than *Signed in*, since no login is read. The Advice strip's *Switch models* line sets a model's window against another model's window of the same length, or the tool-wide window of that length, never a 5-hour share against a month, and names them as the card does ("Kimi K3 5-hour is 90%. GLM-5.2 is 2%. Switch models, not tools."; `AdvisorTests`). Per window, the model closest to its limit leads and names itself with its figures ("Kimi K3 · $1.62 of $3.00"); with two or more models, each also has a window of its own, hidden until revealed in Settings. A model the page gives no limit is shown by its spend with no bar ("$0.40 spent, no published limit"), never against a limit made up for it, and a free model with no limit is left out. With no Go turn on this Mac in the last 31 days the card says so and the reading has no windows. The reading's time is when it was read, not the last turn's: a trailing window read now holds every turn up to now.

What it cannot count: turns on another Mac, or through another tool using the same Go key, since neither is in this database; usage past the limit that Go's *Use balance* charges to Zen credit; a turn on a Go model the page does not list and OpenCode recorded no cost for; and any difference between these rates and what Go actually counts. The page's own console is the vendor's figure.

**Why not Go's own usage figures.** An endpoint that answers them exists, `GET https://opencode.ai/zen/go/v1/usage` with the Go API key, answering `rolling`, `weekly` and `monthly` windows with a percentage and a reset each (slkiser/opencode-quota, `src/lib/opencode-go.ts`, read 2026-09-24). It is not on the Go page, and reading it would mean taking the Go key out of OpenCode's `auth.json` and sending it to an undocumented endpoint. The meter needs neither, so this build does not; if Go documents the endpoint, it becomes the source and this computation its fallback.

### Sessions read from the database

With OpenCode's plugin not reporting, OpenCode's sessions are read from the same database (`OpenCodeSessions`, *Show sessions read from OpenCode's database*, on by default). Every 5 seconds (15 on battery or in Low Power Mode, and not at all while reads are paused) the database's fingerprint is taken — each file's and its write-ahead log's size and modification time — and the sessions are read when it moved or a minute has passed: every session that, or any message of which, was written in the last 30 minutes, with its newest eight messages and, on its own, its newest user message. Both halves of that are deliberate. OpenCode moves a session row's own `time_updated` only at the prompt (`touch` in `packages/opencode/src/session/prompt.ts`, read 2026-09-24), never as the turn's messages are written, so a turn longer than the window is kept in the read by its messages rather than read as vanished mid-turn. And OpenCode writes one assistant message per step of a turn, so a turn of more than eight steps has no user message among its newest eight; the newest user message, read directly, is the turn's clock, which therefore moves only when a prompt is sent and never with the answer's next step. The turn is working when the newest message is the user's, or the assistant's answer is still open, or it closed on a tool call (`finish` `tool-calls`), since OpenCode opens the next step once the tool answers; otherwise it is idle, finished when the answer closed. A working turn nothing has written to for 30 minutes was abandoned and ends without a finish. The turn's clock is the prompt's own time, not the moment the app noticed. A read that fails, a file that is no database, a corrupt one, or one held past SQLite's one-second busy timeout while OpenCode migrates or checkpoints, is a problem rather than a read of no sessions (`sqlite3_open_v2` reads no header, so the first query is where it is found, and a statement that stops before its end is a failure, not the rows so far): nothing changes, the last record stands, and the file is read again when it changes or a minute later, so three working sessions are not ended and restarted from now across one busy second.

Each change between two reads becomes the event a hook would have sent, at the database's own time: a new session and its prompt, the prompt of a new turn, the stop (a failed one when the answer carries an error: `aborted` for the user's own stop, `rate_limit` on a 429), a whole turn that ran between two reads, a subagent's session counted as an agent of the session it works for, an archived session's end, and the end of a working session that vanished. The first read of a run shows sessions as they stand and announces no turn that ended before the app looked. OpenCode's own title for a session is read only while *Show what a session is working on* is on and the screen is not shared, never its "New session - …" placeholder, and lands as the session's name. What it cannot know: a wait. A permission request lives in OpenCode's memory, not its database, so a session read this way is never shown waiting. Each row says *from database*, and the card offers the plugin (docs/hooks.md, *OpenCode*), which reports the wait and the turn's end as they happen; once it has spoken, the reading stands down for the rest of the run, and every session it was still following ends its turn at that moment, unseen and with no finish claimed, as a session that vanished between two reads does, except the one the plugin's first event is about, which that event settles. The card's offer of the plugin, and its line that OpenCode switches to it at its next start, go with the reading: the rows still marked *from database* are ones the plugin will never report.

## Claude Cowork's tasks

Not a figure but a state, held to the same rule: the Sessions card says a Cowork task is working, finished or idle only where it can show where that came from. Cowork runs inside the Claude app and has no hook, so the source is the files the app keeps for each task under `~/Library/Application Support/Claude/local-agent-mode-sessions/<account>/<organisation>/`: the record `local_<id>.json` (its `title`, the first of its `userSelectedFolders`, `lastActivityAt` and `isArchived`, and no other key; the same file holds the system prompt, the account's name and its email address, which are parsed in memory and dropped) and the Agent SDK log `local_<id>/audit.jsonl`, of which only each line's type, its `_audit_timestamp`, a result line's `duration_ms`, `subtype` and `is_error`, and a prompt's `isReplay`, `isSynthetic` and `parent_tool_use_id` are read. Nothing is written, and nothing is read while the Claude app is not running. The code is `Sources/Notchmeter/CoworkSessions.swift` (the reader and the log's grammar) and `SessionTracker.observeCowork` (the rule); `CoworkSessionsTests.swift` pins both. Every such session carries the source `coworkLog` (AgentSession.Source) in the report's `sessions`, the oracle's Sessions card rows and its `peek` lines, where a hook's session says `hook`; on the card the chip says *Cowork*, and its tooltip says where the row came from.

- **Finished** is the log's own `result` line with `subtype` `success`: the turn ended then, and lasted the line's `duration_ms` (the end less the row's start when a line carries none). It is announced only for a turn the app watched run, and never for a result with `is_error` or another subtype, which is an end but not a finish. So a finish is a fact the task wrote down, in the same place as its cost.
- **Working** is inferred, and is the one inference here: the log has a turn open (a prompt, a `user` line in the user's own words, with no `result` after it), it was written in the last four minutes (`CoworkSessions.busyWindow`), and its last line is not a `permission_request` that has stood for three seconds (`askGrace`). The clock starts at the prompt's timestamp when the lines read reach back to it (the last 256 KB of the log, widened once to 8 MB when those hold neither a prompt nor an end), else when the app first saw the turn running. Each line read is held to 64 KB (`lineLimit`), which drops a tool's output and the attachments the app adds to a prompt (an image, a PDF; `isSynthetic`, so not a prompt: on that Mac, the 38 prompt-shaped lines over 64 KB were all such, the longest 17 MB), except the `result` line, read up to 4 MB (`resultLineLimit`): its `result` field is the turn's whole final text, so a long answer makes the end line long, and an end line dropped would be a turn that never ends (working for four more minutes, then idle, with no finish). The longest of the 131 result lines there was 17 KB.
- **Idle** is everything else: no turn open (which is every task with a record and no log yet: the Claude app writes the record, `lastActivityAt` included, as the task is made, before any prompt, so a record alone is no turn), a log quiet for four minutes, or a task stopped at a request. A turn that goes quiet is never claimed as finished; if its end line comes later, that line finishes it.
- **Never waiting.** A `permission_request` line is how a task asks the user something (a tool to allow, or an `AskUserQuestion`): in the logs read on 2026-09-24 on the Mac this was written on, 35 of 36 were followed directly by their `permission_response`, from under a second to nearly seven minutes later, and the 36th ended its log. That is very likely a dialog up in the Claude app, but it has only been read after the fact, never watched while a dialog was up, and the hand on a ring must be a wait something said began. Until it is, a task stopped there is idle.

Why four minutes, measured on 2026-09-24 over the 38 task logs on that Mac (131 finished turns): the log appears to write a model step once it is complete, so a running turn goes quiet while the model writes a long answer. Leaving out the pauses at a request, 141 gaps inside a turn were longer than 30 seconds, 20 longer than two minutes, 4 longer than three and one longer than four (six minutes, after a rate-limit event). Replaying every log through the reader and the rule at a five-second poll, 30 seconds (the window a process-and-transcript watcher uses for Claude Code) dropped a running task to idle 114 times and caught 125 of the 131 finishes; four minutes dropped it 17 times, nearly all at a request, and caught 128, each announced at most five seconds after its end line. The cost is the other side of the same number: a turn stopped with no end line and not at a request (2 of the 38 logs end that way, and a third at a request) reads as working for up to four minutes after its last line.

Unchanged: the Cost card's Cowork spend, read from the same logs by the cost scan and reported under the project "Cowork" (*What is read* above).

## How it reads each tool

| Tool | Where the login comes from | Where the numbers come from |
|---|---|---|
| Claude Code | Keychain item `Claude Code-credentials` (read with prompts off, then through `/usr/bin/security`), else `$CLAUDE_CONFIG_DIR/.credentials.json` or `~/.claude/.credentials.json`, else `CLAUDE_CODE_OAUTH_TOKEN`; a setup on `ANTHROPIC_API_KEY`, Bedrock or Vertex has no plan window and the card says so | `GET https://api.anthropic.com/api/oauth/usage`, unless *Also poll Claude's usage endpoint* is off on Claude Code's page in Settings (Sources), in which case the status line below is the only source and the endpoint is never asked |
| Claude Code cost | — | `~/.claude/projects/**/*.jsonl`, priced per model with the five token buckets (input, output, 5-minute and 1-hour cache writes, cache reads), times 1.1 when the response reports `inference_geo: "us"`; a line's own `costUSD` wins when present; a streamed message's repeated lines collapse by message id to the one carrying the real output count |
| Codex | `auth.json` in `$CODEX_HOME`, else `~/.config/codex`, else `~/.codex` (token read, never refreshed or written); the plan name from Codex's own slug table | `GET https://chatgpt.com/backend-api/wham/usage`, falling back to the newest `rate_limits` line in `~/.codex/sessions/**/*.jsonl` |
| Cursor | `cursorAuth/accessToken` in `~/Library/Application Support/Cursor/User/globalStorage/state.vscdb` | `GET https://cursor.com/api/usage-summary` (falls back to `/api/usage` on request-metered plans), alongside Cursor's own percentages for its models and for other models, which are metered apart from the included total rather than being shares of it (one Enterprise cycle reported 47 % and 100 %, and 147 % does not divide one allowance), and the team pool when the plan has one. A seat whose summary carries `individualUsage.overall` in place of `plan`, no limits, and the two model percentages only in the dashboard's own sentences ("You've used 0% of your included total usage") is read the same way, uncapped on-demand shows as dollars spent with no fill, and when no included or pooled window has a figure, `POST https://cursor.com/api/dashboard/get-current-period-usage` (the dashboard's own cycle figures in cents) fills the included ring from `includedSpend / limit` where the seat has a limit, and otherwise *Today's spend* (today's export total against the average day of the thirty before it; a comparison, so it paces but never runs out, warns or notifies) leads the card; `POST https://cursor.com/api/dashboard/get-sand-usage-status` for the Grok Bot weekly allowance where the seat has one; on by default and switchable off in Settings, `POST https://cursor.com/api/dashboard/get-filtered-usage-events` for the last 30 days of usage events with Cursor's own cost per event ([docs/accuracy.md](accuracy.md)) |
| Gemini CLI | `~/.gemini/oauth_creds.json`, the Google login Gemini CLI caches (token read, never refreshed or written); the calls carry Gemini CLI's own client identity | `POST …/v1internal:loadCodeAssist` for the project and tier, then `POST …/v1internal:retrieveUserQuotaSummary`, else `POST …/v1internal:retrieveUserQuota` for the per-model buckets (a project-scoped refusal retried project-less), on `daily-cloudcode-pa.googleapis.com` then `cloudcode-pa.googleapis.com`, taking the first with a live figure ([docs/accuracy.md](accuracy.md), *Gemini CLI and Antigravity*) |
| Antigravity | the same `~/.gemini/oauth_creds.json` (the app and `agy` keep their own login in the Keychain); the row is shown when `~/.gemini/antigravity-cli`, `/Applications/Antigravity.app` or `~/.antigravity` is present, and its calls carry Antigravity's own client identity | the same three calls, for the session and weekly groups Antigravity's panel shows, on the host Antigravity's CLI logged in `~/.gemini/antigravity-cli/cli.log`, else both hosts as above |
| Kimi Code | `credentials/kimi-code.json` in `$KIMI_SHARE_DIR`, else `~/.kimi`: the OAuth token Kimi Code's CLI keeps (read, never refreshed or written; not sent within thirty seconds of its `expires_at`) | `GET https://api.kimi.com/coding/v1/usages` (the base from `KIMI_CODE_BASE_URL` when it is https on Moonshot's own hosts), the read the CLI's own `/usage` makes: the summary row, the per-window counts and the ratio pools ([docs/accuracy.md](accuracy.md), *Kimi Code*) |
| GitHub Copilot | `oauth_token` in `~/.config/github-copilot/apps.json` (every `github.com` entry tried in turn), then `hosts.json`, then `github.com: oauth_token` in `~/.config/gh/hosts.yml`, the tokens Copilot's editor plugin or `gh` keep (read, never refreshed or written); a token that does not answer falls through to the next | `GET https://api.github.com/copilot_internal/user` with the Copilot client headers the endpoint answers to (`Editor-Version`, `Editor-Plugin-Version`, `User-Agent`, `X-Github-Api-Version`): the read the plugin makes to show its own quota (`quota_snapshots` with `credits_used` since June 2026, or Copilot Free's `monthly_quotas` and `limited_user_quotas`); the credits, a cent each, are folded into the daily history on the day each rise is seen; opt-in, `GET /user/orgs` then `/orgs/<org>/settings/billing/usage/summary` for the month's organisation credits and spend, hidden-by-default windows |
| OpenCode | none: no login is read, and OpenCode's `auth.json` is never opened | `opencode*.db` in `$XDG_DATA_HOME/opencode` or `~/.local/share/opencode` (or `$OPENCODE_DB`), and legacy `storage/message/**/*.json`, read-only: the `message`, `session_message` and `session` tables for turns and sessions; the Go meter is computed from them at the Go page's rates and limits (*OpenCode* above). No network request |
| Claude Code status line | — | Claude Code hands `Notchmeter --statusline` a JSON payload after every turn; the command forwards the context fill, `rate_limits.five_hour`/`seven_day`/`spend_limit`, the session cost, the model and effort, the session id, the folder name, the git branch and pull-request URL when Claude Code sends them, and its `prompt_cache` diagnostics, fast mode, extended thinking, the agent's and the session's names, the lines added and removed, the API time and the repository's host, owner and name ([docs/hooks.md](hooks.md#the-status-line)), over the app's own socket, which takes a line only from a process carrying the app's code signature ([docs/hooks.md](hooks.md#how-it-reaches-the-app)); while a report is under three minutes old the Claude endpoint is not read on the timer, save for a half-hourly read when it carries figures the status line does not ([docs/accuracy.md](accuracy.md#where-claudes-windows-come-from)) |
| Codex reset credits (opt-in) | the same `auth.json` token | `GET https://chatgpt.com/backend-api/wham/rate-limit-reset-credits`, shown and never claimed |

Every window carries its source, and a card tags anything that is not the vendor's own figure: *From Claude Code's status line*, *From rate-limit headers*, *From Codex's last session file* (the newest figure a tool wrote to disk, which may be stale), *Estimate* (the budget) or *computed here* (OpenCode Go's limits, worked out on this Mac from its own turns; `computedLocally`); `--probe --json` carries the same as `source`. Each assistant's page in Settings lists every window of its current reading with its source in words, the endpoint included (*Where each window comes from*: the vendor's usage endpoint, Claude Code's status line, rate-limit headers, a file the tool wrote on this Mac, or worked out on this Mac), beside the login it reads and the requests it makes as the table above states them (`WindowSource.name`, 2026-09-24). The page shows the source a window carries and never a figure, so it adds no number to source; a window whose source is a new kind names that kind there by the same rule. Cost is an estimate at Anthropic's published API rates for the models in your transcripts (`Sources/Notchmeter/ModelPricing.swift`, or Claude Code's own `modelPricing` table and Notchmeter's `pricing-overrides.json` when present); a subscription does not bill this way, it is the API-equivalent value of the work. The card's ranges are Today, Yesterday, Week (since the live Claude weekly window started, with "$1.58 per 1% of weekly"), Month (calendar month to date), 30 days and 90 days, and each applies to every assistant on the card at once: the donut carries one segment per assistant that spent in that range, in that assistant's own colour and sized by its share, with the total in the middle, and the legend names each one with the source it came from, its share and its figure, in the order set under Assistants. An assistant that cannot derive spend from something it publishes is absent, never a zero row ([docs/accuracy.md](accuracy.md)); *In the Cost card* in Settings leaves out any of the others. Cost, Tokens and $/MTok apply across them the same way, each assistant's $/MTok being its own dollars over its own tokens. Once there are five active hours to compare against, the card also shows the last hour against your average active hour over the last 30 days ("Last hour $8.40 · 6x your 30-day average"). The donut, the legend and that burn line are the whole card with *Show details* off, so the panel stays as short with three assistants reporting as with one; details adds the models ranked by cost with their share, the token count and cache-read share ("4.2M tokens · 71% cache reads"), the top projects by the transcript's working directory ("Top: notchmeter $12 · scout $4"), the budget and week lines, the current 5-hour block aligned to the Session meter, named by the clock time it opened and carrying its tokens per minute ("This session block $3.28 since 2:19 PM · 176K/min"), and for 90 days the total since first use. The block names its hour for the reason the week line names its day: the figures above it cover a whole day and this one does not, so a block that has just reset would otherwise read as a contradiction of the day's total sitting beside it. Each assistant's own card carries its spend line ("$118.31 today · $6,600 over 30 days · local transcripts") and, with details on, its 30-day trend; hovering a bar of that trend names the day, its cost and its top model. Only transcripts touched in the last 30 days are read; per-file results and quarter-hour totals are cached in `~/Library/Caches/Notchmeter/` so relaunches are instant and an unchanged file is folded from its totals rather than re-priced (only the current block's transcripts keep their entries, which is all the last-hour and block figures ever ask for), and an append-only daily-totals file in `~/Library/Application Support/Notchmeter/` — beside the drain log rather than in Caches, which the OS may purge and Time Machine leaves out — outlives Claude Code's transcript cleanup, so a day whose transcript is gone keeps the larger figure it was seen with. How the estimate is built, every multiplier it applies, and where it is known to diverge from a bill, is written down in [docs/accuracy.md](accuracy.md), and a golden-transcript test suite pins the numbers. The rate-limit meters, by contrast, are account-wide already: they come from the vendor, not from this Mac's files.

### The drain log

The most common complaint about every meter in this space is "my limit drained abnormally fast and I cannot see why". Notchmeter keeps an append-only log of every successful read per window (`~/Library/Application Support/Notchmeter/drain-log-v1.jsonl`: time, tool, window id, used fraction, reset; never a token; seven days, a few KB a day) and shows "12% → 61% in the last hour" under a meter that moved, a 24-hour sparkline of the main window in each card, and the measured rate feeds the run-out projection and the notifications in place of the even-burn assumption. `--probe` prints the same drain lines.

Antigravity and Gemini CLI meter against the same Google backend, and the login Gemini CLI caches is the only one on disk: the Antigravity app and its `agy` CLI keep theirs in the Keychain and publish quota only through a local server inside the running app, which Notchmeter does not attach to. So both meters need one Google sign-in through Gemini CLI (`gemini`, then *Login with Google*), each asking under its own product's identity; an API-key or Vertex AI setup has no quota to read. The buckets are grouped the way Gemini CLI's own `/stats` groups them, every Gemini model of a tier sharing one pool, and Google declares no window length for them (its docs call them per-day request limits), so these meters carry no pace tick or projection until one is inferred from their resets. Since June 2026 Google serves this endpoint only to Code Assist Standard and Enterprise accounts; a personal account's Gemini CLI row gets a sentence saying so, not an HTTP code. Kimi Code's meter reads the login its own CLI keeps, and only while that login is current; see *Kimi Code*. Adding a tool means one `UsageProvider` actor in `Sources/Notchmeter` and one line in `ProviderRegistry`.

## Who each request says it is

The table *How it reads each tool* says where each figure comes from; this one says what kind of endpoint that is, what each request names itself as, and where it stands against the vendor's own terms. The identities are the constants in each provider's source file, quoted as the code sends them, and every request carries the token of the tool that owns it and nothing else. The kinds are the plain ones: *documented* means the vendor publishes it as an API for this, *undocumented* means it is the request the vendor's own client or dashboard makes, taken from that client's traffic, which the vendor can change or withdraw without notice.

| Provider | What is read | Endpoint kind | Identity sent | The vendor's terms, plainly |
|---|---|---|---|---|
| Claude Code | The session, weekly and per-model windows and extra usage; the cost from this Mac's transcripts | `GET https://api.anthropic.com/api/oauth/usage`: undocumented, the channel Anthropic documents being the status line. The status line: a local command Claude Code runs, no network. `~/.claude/projects/**/*.jsonl`: local files | `User-Agent: Notchmeter/<version>`, `anthropic-beta: oauth-2025-04-20`, and Claude Code's OAuth token as `Authorization: Bearer` ([`ClaudeProvider.swift`](../Sources/Notchmeter/ClaudeProvider.swift)). Its own honest name, nothing borrowed | Anthropic's published policy (on Claude Code's [Legal and compliance](https://code.claude.com/docs/en/legal-and-compliance) page) says consumer OAuth is "designed to support ordinary use of Claude Code and other native Anthropic applications", and Notchmeter is neither. Turning off *Also poll Claude's usage endpoint* leaves the status line as the only Claude source, and then no request goes to Anthropic at all. The full quotes and the app's reading of them are in [privacy.md](privacy.md#privacy-and-terms) |
| Codex | The session, weekly or monthly windows; the reset credits, opt-in | `GET https://chatgpt.com/backend-api/wham/usage` and, opt-in, `/wham/rate-limit-reset-credits`: undocumented, the backend Codex itself asks. The `rate_limits` lines in `~/.codex/sessions/**/*.jsonl`: local files, the offline fallback | `User-Agent: Notchmeter/<version>`, Codex's token as `Authorization: Bearer`, and `ChatGPT-Account-Id` when `auth.json` carries one ([`CodexProvider.swift`](../Sources/Notchmeter/CodexProvider.swift)) | Broad language on automated access, nothing that names a read-only status request; the request says what it is |
| Cursor | Included usage and on-demand spend for the cycle, the team pool, the dashboard's cycle figures, the Grok Bot allowance, and (on by default) the last 30 days of usage events | `GET https://cursor.com/api/usage-summary` (else `/api/usage`), and `POST` to `/api/dashboard/get-current-period-usage`, `/api/dashboard/get-sand-usage-status`, and, with usage events on, `/api/dashboard/teams` and `/api/dashboard/get-filtered-usage-events`: undocumented dashboard endpoints, their shapes taken from cursor.com's own traffic. The login comes from Cursor's `state.vscdb`, a local file | `User-Agent: Notchmeter/<version>`; the editor's access token as the `WorkosCursorSessionToken` cookie, the form the dashboard sends it in; `Origin: https://cursor.com` on the `POST`s, which Cursor checks against cross-site requests ([`CursorProvider.swift`](../Sources/Notchmeter/CursorProvider.swift)) | Cursor's [Terms of Service](https://cursor.com/terms-of-service) (effective 2026-09-03, read 2026-09-24) list among the things a user may not do: "harvest, scrape, or extract data from the Service". These are the dashboard's own reads of your own usage, made by a program rather than the browser; whether the clause reaches them is Cursor's to say, and the commitment below applies |
| GitHub Copilot | The month's AI-credit allowance (or a legacy seat's premium requests, and the chat and completion quotas where a plan meters them); organisation billing, opt-in | `GET https://api.github.com/copilot_internal/user`: undocumented, the read Copilot's editor plugin makes to show its own quota. Opt-in, `GET /user/orgs` and `/orgs/<org>/settings/billing/usage/summary`: GitHub's public REST API | The quota read: `User-Agent: GitHubCopilotChat/0.26.7`, `Editor-Version: vscode/1.96.2`, `Editor-Plugin-Version: copilot-chat/0.26.7` and `X-Github-Api-Version: 2025-04-01`, the Copilot Chat extension's identifiers, because the endpoint is keyed on them and answers no other. The organisation reads: `User-Agent: Notchmeter/<version>` and `X-GitHub-Api-Version: 2022-11-28`. The token as `Authorization: token` ([`CopilotProvider.swift`](../Sources/Notchmeter/CopilotProvider.swift)) | Broad language on automated access. The quota read goes under another client's name, which is the one thing here a vendor could fairly call borrowed, so it is stated here and in [privacy.md](privacy.md) rather than left in the code |
| Gemini CLI and Antigravity | The per-model quota Gemini CLI is metered on; the session and weekly groups Antigravity's panel shows | `POST …/v1internal:loadCodeAssist`, `:retrieveUserQuotaSummary` and `:retrieveUserQuota` on `daily-cloudcode-pa.googleapis.com` and `cloudcode-pa.googleapis.com`: undocumented, the internal Code Assist calls Gemini CLI and Antigravity make at every start. `~/.gemini/oauth_creds.json` for the login and `~/.gemini/antigravity-cli/cli.log` for Antigravity's host: local files | The Antigravity row: `User-Agent: antigravity`, `Client-Metadata: {"ideType":"ANTIGRAVITY","platform":"MACOS","pluginType":"GEMINI"}` and `ideType: ANTIGRAVITY` in the `loadCodeAssist` body, because the backend picks which product's licence to check from the caller's identity. The Gemini CLI row: `User-Agent: Notchmeter/<version>`, with `ideType: GEMINI_CLI` in that body. The Google token as `Authorization: Bearer` ([`CodeAssistProvider.swift`](../Sources/Notchmeter/CodeAssistProvider.swift)) | Broad language on automated access. As with Copilot, the Antigravity identity is another client's, sent because the endpoint answers no other, and said here |
| Kimi Code | The five-hour window, the week and the monthly pools | `GET https://api.kimi.com/coding/v1/usages`: undocumented as an API, the read Kimi Code's own CLI makes for its `/usage` command. `credentials/kimi-code.json` in `~/.kimi` for the login: a local file | `User-Agent: Notchmeter/<version>` and the CLI's OAuth token as `Authorization: Bearer` ([`KimiProvider.swift`](../Sources/Notchmeter/KimiProvider.swift)). Its own honest name, nothing borrowed | Broad language on automated access; the request says what it is |
| OpenCode | OpenCode's recorded turns and sessions, and the Go meter computed from them | No request of any kind: OpenCode's database on this Mac, opened read-only | Nothing is sent, and no token is read | Nothing of OpenCode's is asked for; the one Go endpoint that exists is undocumented and is not called (*OpenCode* above) |

None of these requests signs in, refreshes a token, or asks a model anything; each is a read of the account's own usage, on a five-minute base cadence that only ever slows down ([privacy.md](privacy.md), *How often*).

## If a vendor asks us to stop

If a vendor asks the project to stop reading its service, whether through a GitHub issue, the contact in [SECURITY.md](../SECURITY.md) or any other channel, that vendor's meter is removed in the next release, and the release notes say so and why. The app is not argued back in through another route: a meter that loses its endpoint reports the error and waits, and one the vendor has asked us to drop goes. Until that release is out, switching the assistant off in Settings › Assistants stops every request to it at once.

## Send Feedback's link

Send Feedback hands a GitHub issue to the browser as a link, and a link can only carry so much, so the sheet cuts the text to a cap and says what it cut (`Feedback.payload`). The cap is not GitHub's own limit on a link. GitHub's documentation names no figure and the server cuts a link off at 8,191 bytes ([github/docs#5136](https://github.com/github/docs/issues/5136), 2021), but a browser that is not signed in to GitHub never reaches the issue page with the link: it is sent to `https://github.com/login?return_to=` with the whole link percent-encoded once more as the return address, and that page fails first. Measured 2026-09-24 with curl against this repository's new-issue page, signed out, with a link of plain letters and one of `%20` escapes:

| Link | Bytes | Issue page | Return address | Sign-in page |
|---|---|---|---|---|
| plain | 4,500 | 302 | 4,561 | 200 |
| plain | 6,500 | 302 | 6,561 | 200 |
| plain | 6,900 | 302 | 6,961 | 500 |
| plain | 7,000 | 500 | | |
| escapes | 4,500 | 302 | 7,505 | 500 |
| escapes | 5,298 to 6,498 | 302 | 24 (a bare `https://github.com/login`; the text discarded) | 200, on an empty sign-in page |
| escapes | 6,900 | 500 | | |

A finer pass on the same day put the sign-in page's edge between a return address of 6,928 bytes (200) and 6,953 (500). So the figure that counts is the return address, and the sheet measures a new-issue link that way (`Feedback.loginRedirectLength`: 35 bytes of prefix, the link, and two more bytes for every `%`, `:`, `/`, `?`, `=` and `&` in it, which the second encoding turns into three) and holds it to **6,500 bytes** of return address (`Feedback.browserLimit`), 400 under the edge. The formula reproduces both measured addresses exactly (4,561 and 7,505; `FeedbackLinks` pins them). Text that encodes heavily is held shorter than text that does not: a character of Japanese is nine bytes in the link and fifteen in the return address. A signed-in browser skips the redirect and would take a longer link, but the app cannot tell which browser it is handing the link to, so every browser gets the shorter one. A `mailto:` link is held to 8,000 bytes of its own (`Feedback.mailtoLimit`), the figure every link was held to before the redirect was measured: mail apps publish no limit, and nothing has shown one taking less. Mail's compose window takes the text directly and is never cut.

## The golden tests

`Tests/NotchmeterTests/CostGoldenTests.swift` feeds transcript excerpts, written inline as JSONL, through the same `parseFile → dedupe → summarize` path the app uses, with a fixed `now`, and pins each total to nine decimal places:

| Fixture | Rule under test | Pinned total |
|---|---|---|
| Line carrying `costUSD: 0.42` with `inference_geo: "us"` | own cost wins, no multiplier | $0.42 |
| Sonnet 5 line with 5m and 1h cache tiers | five buckets at list | $0.0275 |
| Opus 4.6 line with `inference_geo: "us"` | ×1.1 on every bucket | $0.14575 |
| Three lines of one Fable 5.1 response (output 3, 3, 1090) | keep the real output count | $0.1095 (not $0.05515) |
| `<synthetic>` line | $0, not listed | $0 |
| `claude-nimbus-1` line | $0, listed as unpriced | $0 |
| Sonnet 5 line 53 days old | outside the window | $0 |
| All of the above in one transcript | sum, last hour, unpriced set | $0.70275 total, $0.67525 in the last hour |

The same file unit-tests the residency multiplier, the dedupe choice, the average active hour (including the bursty month a median gets wrong) and the burn multiple's five-hour and non-zero guards, and the `CostScanning` suite in `ProviderParsingTests.swift` covers cache-tier parsing, model-id normalisation and day bucketing. These totals are pinned and do not move: adding the other tools to the Cost card changed none of them.

`OpenCodeTests` does the same for OpenCode against real SQLite files in the tables OpenCode's migrations create: the four pricing rules (a Go turn at the page's rate whatever OpenCode recorded, a recorded cost winning above zero, a subscription zero at Anthropic's and OpenAI's list rates, a local model's zero unpriced and named), the page's rows, limits, peak hours, long-context tiers and promotion, the buckets (reasoning back into output), one turn counted once across two databases and never from both tables, legacy files, the file left byte for byte as it was with no sidecar created, the three Go windows against their shares with the tightest model leading and a model with no published limit shown without a bar, and the sessions read from the database replayed into the tracker with the turn's own length, a turn longer than the read window kept in it by its messages, a long turn's steps read as steps and never as prompts, a file that is no database reported as a problem and a failed read changing no session, and the plugin's first event ending the turns the reading was still following. `AdvisorTests` pins the Go model advice, window for window, in the card's own names.

`CodexCostTests` does the same for Codex over synthetic rollouts — $0.0329 for a 10,000-input/8,000-cached/2,000-output `gpt-5.3-codex` turn, $0.011375 for the `gpt-5.1-codex` fallback, a replayed `response_id` counted once, an event-only rollout, a cache write priced at nothing, an unknown model at $0 and named, and a turn with no model at $0 with a reason. `CostEngineTests` pins that the top figures are the rows added up, that a tool with nothing to say leaves the others alone, and that a failed vendor read keeps its own timestamp and error without touching another tool's scan. Run them with `scripts/test.sh`.

## Reproducing a number

From the terminal, without the app running and without a Keychain prompt:

```bash
swift run Notchmeter --probe --no-prompt
```

The last lines are the cost summary, the totals first and then one clause per tool that reported spend: `cost: today $… yesterday $… 30d $… last hour $… (Nx the 30-day average $… per active hour) … claude today $… 30d $… (localTranscripts) codex today $… 30d $… (localSessions) unpriced=[…]`. A tool with no clause reported nothing, which is not the same as reporting zero. To check a single transcript by hand, list its priced lines and apply the rules above:

```bash
jq -c 'select(.message.usage) | [.timestamp, .message.id, .message.model, .message.usage]' \
  ~/.claude/projects/<project>/<session>.jsonl
```

Group by the id column, keep the line with the largest `output_tokens` in each group, price the five buckets from the table, multiply by 1.1 where `inference_geo` is `"us"`, and sum. Claude Code's `/usage` Session block will not match this figure and is not meant to: it covers one session since its last `/clear`, while the card covers every transcript on the Mac for the day.

## Where Claude's windows come from

Claude Code's session (5-hour) and weekly windows have two sources, taken in this order:

1. **Claude Code's status line**, when it is installed ([docs/hooks.md](hooks.md#the-status-line)) and its last report is under three minutes old and describes no window that has since reset (`Statusline.Message.standsIn`). Its `rate_limits.five_hour` and `seven_day` (and, behind a Claude apps gateway, `spend_limit`) are the figures on the ring, marked *From Claude Code's status line*. It is local and costs no request, so while it is fresh the timer does not read the endpoint at all.
2. **`GET https://api.anthropic.com/api/oauth/usage`**, the fallback, on its own five-minute cadence whenever there is no fresh status-line report (and, beside a fresh one, only for what the status line cannot carry, as the next paragraph sets out): the status line is not installed, no turn has run for three minutes, or the report carries no rate-limit window at all.

The endpoint answers more than the status line has fields for: the per-model weekly limits (`limits[]` of kind `weekly_scoped`, and the older `seven_day_opus` and `seven_day_sonnet`), the extra-usage spend against its monthly cap, and the weekly window itself when a payload leaves it out. Those are neither dropped nor left to age. While the status line is fresh the endpoint is still read every thirty minutes, a sixth of its own cadence, but only while the last reading holds a figure the endpoint alone supplied; an account whose endpoint says nothing the status line does not is never read beside it. The first such read of a run is taken at once, since nothing says how old the cached extra figures are; and with nothing from the endpoint on record at all, one read is taken to learn which kind of account it is. The answer is laid *under* the status line's windows: the session and weekly figures stay the status line's, and only the others are refreshed (`PollingPolicy.endpointDue`). A read beside the status line that fails costs only those figures until the next one; the card keeps the status line's reading and is not marked failed.

A Refresh the user asks for (the Refresh button, the ring, a card's menu) reads the endpoint even beside a fresh status line, because the status line cannot be asked for a new figure, only waited for; the answer is merged the same way. With *Also poll Claude's usage endpoint* off (Settings › Assistants › Claude Code) the endpoint is never read, on any of these paths. Pinned by `StatuslineFirstTests`.

## Why there is no header fallback

Anthropic attaches rate-limit headers to some responses for OAuth accounts: `anthropic-ratelimit-unified-5h-utilization` and `-7d-utilization` (a 0–1 fraction of the window), `-5h-reset` and `-7d-reset` (epoch seconds), plus `-overage-utilization` and `-representative-claim`. They describe the same Session and Weekly windows the usage endpoint reports, so they look like a way to keep the meter alive if the usage endpoint ever changes.

Checked on 2026-09-01 against [OpenUsage issue #1098](https://github.com/robinebers/openusage/issues/1098), which records the requests and their responses verbatim:

- `GET https://api.anthropic.com/api/oauth/usage` (with `anthropic-beta: oauth-2025-04-20`) does **not** carry the headers. Its answer is the JSON body Notchmeter already parses.
- `POST https://api.anthropic.com/v1/messages/count_tokens` does **not** carry them either.
- The headers arrive only on `POST https://api.anthropic.com/v1/messages`, the inference endpoint. The probe recorded there sends a Haiku request with `max_tokens: 0`, and the issue measures it at roughly eight input tokens of Haiku per call.

So the only request that yields the headers is an inference request made with Claude Code's OAuth token. That is exactly the call Notchmeter promises never to make: it consumes model capacity, it would appear in Anthropic's records as third-party inference with a Claude Code credential, which the [Claude Code legal page](https://code.claude.com/docs/en/legal-and-compliance) restricts and Anthropic began enforcing server-side in 2026, and it would move the very Session number the meter reports. Eight tokens per poll is small, but the meter's standing rests on being read-only, and there is no read-only request that returns the headers.

OpenUsage itself does not read them: its `ClaudeUsageClient.swift` fetches the usage endpoint and the profile endpoint only, its `ClaudeUsageMapper.swift` maps the JSON body, and the fallback it ships for tokens without the `user:profile` scope is its local transcript scanner for spend, not a header probe (both files read from `main` on 2026-09-01).

What Notchmeter does instead, in `ClaudeProvider.swift`:

- Every response from the usage endpoint is checked for the unified headers (`rateLimitWindows(from:)`, unit-tested in `ProviderParsingTests.swift`). If the JSON body is missing or unreadable but the headers are present, the Session and Weekly windows are built from the headers and the meters carry the note "From rate-limit headers"; per-model weekly limits and extra usage are unavailable in that mode. Today this path never fires, because the endpoint does not send the headers; it is there so that a change on Anthropic's side degrades to coarser numbers rather than to an error.
- If the usage endpoint answers with an error and no headers, the meter reports the error and waits. It does not probe `/v1/messages`.

Should Anthropic publish a read-only endpoint that returns the headers, or add them to the usage endpoint's response, the parser is already in place.
