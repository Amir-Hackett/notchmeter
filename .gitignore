.build/
build/
dist/
# --render-assets draws its review-only pictures here (AssetRenderer.render), beside the README's.
docs/media/review/
.swiftpm/
*.xcodeproj
.DS_Store

# Claude Code keeps its per-task git worktrees here; they are checkouts, never content.
.claude/worktrees/
